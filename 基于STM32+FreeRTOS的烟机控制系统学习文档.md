### 4.3.4.1 驱动层代码编写顺序

￼
下边说一下笔者做该项目时从0-1的编码顺序，以供大家参考。总体顺序为驱动层--中间层--应用层，本小节为驱动层的编写顺序，4.3.5和4.3.6章节分别为中间层和应用层

#### 电机驱动层

￼
首先，对于笔者而言，最难的驱动层部分是电机驱动。电机驱动又分为电机旋转和编码器测速两部分，因此先从最简单的电机旋转代码开始编写。简而言之，电机驱动就得先让电机转起来再说。先创建一个Keil的裸机工程，里边包含两个.c文件--main.c（保存在USER文件夹中）和motor.c（保存在BSP文件夹中），以及增添对应的头文件。然后开始编写motor.c文件，应编写如下函数：电机的转动初始化函数、电机启动函数、电机停止函数、电机方向和速度控制函数；编写完之后，再编写main.c文件，在main函数里边调用这些函数，最后进行调试，看各个函数是否能达到期望。这一步骤在企业当中，也叫做单元测试。
测试通过后，继续加功能--编码器测速。你需要在motor.c文件中添加以下函数：编码器功能初始化、定时器中断初始化函数（用于计算转速）、获取编码器计数值函数、获取电机转速函数以及获取电机转向函数。
◦

#### 传感器驱动层

￼
该驱动层分为MQ2气体浓度传感器驱动以及DHT11温湿度传感器驱动。继续在裸机工程的BSP文件夹下创建一个MQ2文件夹和DHT11文件夹，里边分别创建对应的.c/.h文件。对于mq2.c文件，需编写如下函数：MQ2初始化函数（ADC）、获取ADC原始值函数、获取气体浓度值函数、气体浓度阈值检测函数；对于dht11.c文件，需编写以下函数：dht11初始化函数、读数据、采集数据函数。
￼

#### ￼串口及LCD驱动层

￼
编写完之后继续调试，看传感器是否输出气体浓度数据、温湿度数据。在该过程中，你发现要看这些数据，需要用到串口以及LCD进行数据显示，因此需要在BSP文件夹下创建USART以及LCD的.c/.h文件，至于具体的函数由于太过基础，此处不再过多叙述。
PS：此处需要注意的是，如果驱动层代码你能移植过来，则移植即可。只要搞清楚移植过来的代码的原理以及驱动的步骤就行。
◦

#### SPI驱动层

在编写LCD驱动层过程中，由于LCD与主机的通信方式是SPI通信，因此在BSP文件夹下创建SPI文件夹，添加对应的.c/.h文件。之后再配合LCD进行调试。
◦

#### 蜂鸣器驱动层

￼
在BSP文件夹下创建BEEP文件夹，添加对应的.c/.h文件，需要包括蜂鸣器的GPIO初始化函数、开启、关闭以及长鸣、短鸣函数。依然在main函数中调用对应的驱动函数进行调试，看是否能发声。
◦

#### 按键驱动层

￼
在BSP文件夹下创建KEY文件夹，添加对应.c/.h文件，需要包括按键消抖、长按检测、短按检测等函数，之后进行调试，如按键按下，电机转动，继续按下，则电机停止。总之一句话，当功能越加越多，就要有意识的进行联调！
◦

#### PID算法驱动层

￼
在BSP文件夹下创建PID文件夹，编写对应的.c/.h文件。编写好之后开始调试，调试的方法是用PID控制电机，通过串口加上位机，确立合适的kp、ki、kd系数，以达到目标转速和实际转速相符合的目的。
◦

#### 风速算法驱动层

￼
在BSP文件夹下创建WIND文件夹，编写对应的.c/.h文件，之后进行调试。多说一点的是调试之前，请用条件编译注释掉main函数里比较耗时的一部分函数。因为我们现在是裸机工程，所有的应用函数都在排队等着执行，如果前边函数占用时间太久，可能会导致阻塞现象。因此先注释掉一部分阻塞代码以确保正常调试。调试的期望是能观察到串口输出对应的风速数据。
◦

#### Bootloader驱动层

￼
该驱动层包括IAP驱动、Flash驱动、DMA驱动。先在BSP文件夹下创建IAP、STMFLASH、DMA文件夹，以及增添对应的.c/.h文件，然后开始调试。先调试串口加DMA方式能否接收到固件，再调试Flash的擦写以及跳转APP操作是否成功。
◦

#### CRC32算法驱动层

￼
等待Bootloader功能通过以后，在BSP文件夹下创建CRC32文件夹，编写对应的.c/.h文件。然后与Bootloader功能联调，期望是通过校验后的固件才能执行Bootloader功能，并且通过串口输出校验信息。

### 4.3.5 移植rtos并进行内核裁剪

￼
编写完所有的驱动层代码之后，在4.3.4的裸机工程基础上，将rtos移植进去，并自己进行内核裁剪与配置。如果不会，请参阅第7.1章节—rtos的移植与内核裁剪。
移植好之后，开始编写中间层代码。实际上中间层代码，就是调用中间层的API来搭建框架，这个框架对底层提供接口，对上层屏蔽底层细节。你需要在工程中新创建一个文件APP_TASK，然后编写对应的.c/.h文件。在C文件中添加RTOS的各个任务函数，暂时不用实现各个任务的细节。

### 4.3.6 参照源码编写应用层代码

￼
完成上一步后，开始编写应用层代码。如何编写呢？你需要根据5.3章节的详细功能需求，按功能模块顺序思考对应的代码架构，然后再实现各个任务的细节。以传感器控制功能模块为例，可以看到有好几个模式，那就可以想到在一个任务中采用状态机架构去实现每种模式的功能。
如果你觉得过于困难，请参考笔者的代码工程中的应用层代码app_task.c文件。到这一步完成了整个复现步骤的95%了，剩下的5%还是建议按照4.2章节进行操作。

## 项目框架

￼

### 5.1 硬件系统架构

￼

￼![image-20260611225111317](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225111317.png)
￼

烟机硬件系统架构如上图所示。整个项目所用到的MCU外设包括GPIO、ADC、高级定时器PWM互补输出、SPI、USART、DMA、高级定时器编码器测速；由于任务较多，为减少裸机轮询时的长时间CPU占用阻塞，采用开销较小的FreeRTOS操作系统作为中间层，合理调度CPU资源，便于系统实时控制；最后，为解决设备部署后更新困难问题，开发带CRC校验功能的Bootloader，通过开发Python脚本，处理原始固件得到附带CRC编码新固件，最后通过串口＋DMA中断方式下载进STM32端，完成升级跳转。

### 5.2 软件系统架构

￼
如下图所示，整个软件系统架构通过FreeRTOS作为中间层来进行资源划分，分为数据层软件架构和bootloader升级软件架构。
数据层软件架构除去开始任务及中断级任务外，共计7个任务，而Bootloader任务（prior7）在boot升级软件架构中。数据层任务分别为：传感器采集任务（prior3）、风速计算任务（prior3）、直流有刷电机驱动任务（prior5）、UI显示任务（prior1）、防回流任务（prior2）、按键扫描任务（prior4）及编码器测速任务（prior6）。Boot升级软件架构分为PC端和设备端，先将APP代码编译后生成原始bin文件，再通过Python脚本处理该文件得到附加CRC码的新固件bin文件，然后将新固件通过串口调试工具，由PC端发送到设备端；设备端的接收驱动采用的是DMA的方式，而不是传统的串口中断接收，这样做的好处是无需CPU的参与，能减少CPU的开销。接收到新固件后，将新固件一分为二，分为不带CRC码的原始数据及CRC码（占4字节）。设备端需要先对原始数据重新计算新的CRC码，然后与上一步分出的CRC码相比较，比较相同才能进入下一步--固件升级。

![image-20260611225242456](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225242456.png)

![image-20260611225253899](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225253899.png)

## 5.3 详细功能需求拆解

￼

### 5.3.1 传感器控制功能

￼![image-20260611225318002](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225318002.png)

￼
￼

如上图，传感器控制功能分为自动模式/防回流模式/待机模式、风速算法输出。建议大家在看这一部分时，看到功能需求，就要有一个本能反应—自动在脑海里拆解出需要用到的MCU外设以及所要用到的硬件。比如，对于传感器控制功能需求，我看到的硬件有：有刷电机、温湿度传感器、烟雾浓度传感器、MCU、按键；看到的可能要用到的MCU外设有：定时器、GPIO、外部中断、PWM、ADC。并且，如果大家做项目做多了之后，最好是看到需求，脑海里也能立刻想到一些软件架构，比如说此处至少有三种模式，那我们就可以想到用状态机等等。
此处，笔者需要多说一点的是，当你拆解出一个功能需求的具体的技术栈之后，你不要急着往下做。此时可以先动手写驱动代码，比如把电机驱动起来、把传感器驱动起来等；一定要先搞定驱动层，保证底层代码没有问题，在往上层构建代码。这样做的好处有两个：第一，减少你调试的时间；第二，就算遇到了问腿，调试的时候也不用往底层抠寄存器，专注于上层。
等把一个功能调通之后，再接着加功能，一定要让自己处于学习的拉伸区，从不难到一点点难慢慢过渡，这样你的收获也是最大的，极大降低个人的畏难情绪！此外，对于基础比较好的同学，如果你已经对相关驱动代码非常熟悉了，那你可以为加快效率，使用AI Agent帮你编写，但一定要注意自己进行代码评审！

### 5.3.2 手动烟机控制功能

￼

￼![image-20260611225416639](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225416639.png)
￼

PS：该功能需要与5.3.5节的电机控制环功能配合。

### 5.3.3 人机交互功能

￼
（1）UI显示功能：采用LCD显示当前工作模式、风速值、风机转速挡位、及当前电机转速、cooking event时间、delay off时间。
（2）按键提示音：在通过按键切换工作模式时，采用有源蜂鸣器发声。如果短按一下，蜂鸣器鸣叫一声；如果是长按，蜂鸣器一直持续鸣叫，直到按键松开。

### 5.3.4 固件升级功能

￼
（1）先通过Python脚本处理APP固件生成附带CRC检验码的bin文件
（2）通过串口加DMA接收带CRC检验码的APP固件；
（3）进行CRC32检验，检验通过则进行下一步，否则失败需要重传
（4）编写Bootloader任务，将APP程序从RAM写到Flash上；
（5）实现Flash分页擦写；
（6）设置SP及PC寄存器，重定向中断向量表；
（7）跳转到APP程序执行。

### 5.3.5 电机速度环控制

￼
采用高级定时器的编码器功能测量电机转速。如选择挡位为low时，此时理论转速应该为190 PRM，但由于电机带负载，实际转速会与理论转速存在偏差，因此在这种情况下，采用位置式PID算法，将PID输出作为调速的PWM占空比，从而去调节电机转速，以减小理论值与实际值之间的差距。

### 5.3.6 可扩充功能

￼
可能有同学会问，如果都拍了此项目，如何减少“撞车”的风险呢？因此本小节提出以下可扩充功能给大家进行参考（如果有同学不想折腾或者基础较差，那可以不选择实现以下扩充功能）。
（1）WIFI/BLE功能：具体来说，可以用ESP32或者是蓝牙模块+手机端APP来增加无线功能。比如：第一，本项目用到两个按键来进行人机交互，那大家可以选择使用APP来进行按键操作（在APP上添加按键组件），或者是将风速以及传感器的数据上传到APP进行显示等。
第二，可以做OTA功能，OTA指的是over the air，以无线的方式将固件传输到设备端进行升级（本项目采用的是IAP升级方式）。具体做法示例为：电脑上插蓝牙适配器，在PC端通过上位机将固件传输到设备端蓝牙模块的串口，然后将蓝牙模块的串口与STM32的串口连接即可，串口这边的驱动函数还是可以延用本项目中的USART+DMA方式；亦或者不用蓝牙模块，改用ESP32，通过MQTT协议将PC端的固件传输到ESP32的串口，同理ESP32的串口也需与STM32的串口相连接。具体的APP怎么实现以及相应的物联网案例可查阅笔者之前开源的物联网RTOS项目，已放到学习资料--物联网RTOS开源项目文件夹下。此外，你也可以利用强大的AI，将上述我说的思路喂给AI，让它帮你实现。
（2）光敏传感器自动照明功能：原本功能需求里边还有一个照明控制功能，即在厨房光线过暗的情况下，自动打开灯光照明。但考虑到此功能较简单，并没有实现在项目中，如果有同学觉得需要添加的，请自行添加，具体驱动代码见物联网RTOS开源项目。
（3）低功耗Tickless功能：该功能原在笔者微波炉项目中做过演示，具体来说，当烟机切换到待机模式下，此时可以进入低功耗模式，以降低功耗。具体原理见笔者帖子：FreeRTOS的低功耗Tickless模式_freertos tickless-CSDN博客
（4）LVGL或者ewmin功能：由于笔者在本项目中仅采用LCD显示屏显示相关数据信息，大家可以对其进行优化。即采用LVGL或采用ewmin来做UI界面。但需要注意的是，所使用的STM32板子的Flash容量大小必须大于64 KB。而在本项目中，笔者所用的板子为STM32F103C8T6，其Flash容量大小为64 KB，是不足以负担LVGL驱动库。因此，若大家要实现此功能，建议使用STM32F103ZET6板，代码是可以通用的，记得将中等容量的启动文件变成高等容量的启动文件即可。其对应参考资料在学习资料文件夹—LVGL手册。
（4）无刷电机控制（带FOC算法）功能：笔者本项目使用的是直流有刷电机，大家也可以选择无刷BLDC电机来替换，并采用FOC算法来进行控制。因为BLDC电机的基本控制是采用六步换向的方法，但此方法每次驱动电机仅能转固定的角度，如果在高精度场景下并不适用；因此就有了FOC算法来对电机进行像素级别的控制，让电机转动的更加丝滑。
不过对应的标准库驱动函数需要自己编写（因为本项目的电机驱动代码是笔者纯手搓的，底层代码不敢用AI），如果你想选择HAL库，那整个项目的驱动你都需要换成HAL库，工作量过大，请谨慎考虑！对应参考资料在学习资料文件夹--DMF407电机控制专题教程_V1.0.pdf。此处解释一下笔者为什么不采用BLDC电机，原因无他，太贵了，一个无刷电机驱动板要两百多，已经是有刷电机的一倍多成本。

6. ## 硬件原理实现

  ￼

  ### 6.1 直流有刷电机驱动板电路

  ￼
  直流有刷电机驱动板电路比较复杂，分为电源系统电路、电流检测电路、电压检测电路、温度检测电路、过流保护电路、H桥驱动电路以及编码器电路等多个模块。但在本项目中实际用到的功能是驱动以及编码器测速，因此笔者仅对H桥驱动电路、编码器电路做分析。剩余电路模块，如有兴趣，请点击下方链接自行学习：Motor-1 Gitee工程资源-详细使用指南_20250120175925.pdf · gaoyanzeng/drg_motor_1_stm32f407igt6 - Gitee.com.

  ### 6.1.1 H桥驱动电路分析

  ￼
  先来看简单的H桥驱动分析（第7章节还会再出现），如图6.1所示：

￼![image-20260611225606181](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225606181.png)
￼

图6.1
如上图H桥的原理表述为：Q1-Q4都是NPN型三极管，当Q1和Q4的基极给高电平、Q2和Q3的基极给低电平，此时Q1和Q4导通（原理看模电知识，此处不过多解释），那么此时电流经VCC流经Q1-->电机-->Q4-->GND（即从a-->b），此时电机转动（假设正转）；而当Q1和Q4的基极给低电平、Q2和Q3的基极给高电平，此时Q2、Q3导通，Q1和Q4截止，电流方向相反（从b-->a），电机反转。
理解上述简单H桥驱动电路原理后，再来看实际的H桥电路分析，如图6.2所示：

￼
￼![image-20260611225623955](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225623955.png)

图6.2
首先给EL0631光耦（上图中序号2）输入两路相反的控制信号，0代表低电平，1代表高电平，其中PWM_UH为0，PWM_UL为1。控制信号经过光耦的反相之后进行输出，此时两路信号的高低电平状况就和之前相反了。反相后的两路信号分别输入到上、下两个IR2104S半桥芯片的IN引脚（上图中序号3）。
序号3的IR2104S半桥芯片的IN和SD引脚是信号输入引脚，HO和LO是信号输出引脚，输入信号和输出信号的逻辑如下表6.2.1所示：

￼![image-20260611225640339](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225640339.png)

表6.2.1
从表中我们可以得出三个结论：1、只要SD引脚（接的PA2）输入低电平，所有的输出都会停止，即HO和LO都输出低电平；2、当SD引脚为高电平时，HO引脚输出电平的高低状况和IN引脚是一致的；3、HO和LO引脚永远不会同时输出高电平，这就保证了H桥上同一侧的MOS管不会同时导通，从而避免短路事故的发生。假设SD引脚输入了高电平，我们根据逻辑表，结合此时上下两个半桥芯片的IN引脚输入电平，就可以得出上下两个半桥芯片的输出电平状况（上图中序号4）。此时Q1和Q4导通，Motor+接到了POWER（VCC），Motor-接到了GND，电机就可以正常工作了（假设此时电机正转）。

### 6.1.2 编码器电路分析

￼
￼如图6.3所示：

￼![image-20260611225807848](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225807848.png)
￼

图6.3
CN2为6P HT396R端子，用于连接电机的编码器，可以看到，编码器信号经过RC低通滤波，以得到更准确的信号。而有的直流有刷电机编码器是霍尔传感器搭建的，这种霍尔编码器信号较弱，所以接了上拉电阻，以稳定输出高电平信号。编码器供电方式有2种：5V（VCC5）和 11V（VCC11），需要根据电机编码器参数选择合适的电源，过高电压可能烧毁编码器，一般使用5V即可。

### 6.2 DHT11电路分析

￼

￼
￼![image-20260611225832916](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225832916.png)

图6.4
DHT11电路图如图6.4所示，该传感器的电源输入为3.3-5 V，本项目中采用的是3.3 V的电源输入。其中，D3是电源指示灯，由于二极管的单向导电性，此处指示传感器的通电状态。下边说一下DATA引脚，DATA用于MCU与DHT11之间的通讯和同步，采用单总线数据格式，一次通讯时间4ms左右，数据分小数部分和整数部分，具体格式为：8bit湿度整数数据+8bit湿度小数数据+8bit温度整数数据+8bit温度小数数据+8bit。
最后，DATA引脚接了一个4.7K的上拉电阻，用于代表总线空闲状态为高电平。当MCU发送开始信号后，由上拉电阻将总线拉高表示切换到输入模式。此外，关于DHT11协议的详细分析，有兴趣请看学习资料文件夹—DHT11说明书最新版。

### 6.3 MQ2电路分析

￼

￼![image-20260611225917498](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225917498.png)
￼

图6.5
MQ2传感器的电路图如图6.5所示，其适用于液化气，苯，烷，酒精，氢气，烟雾等气体的检测。具有灵敏度高，响应快，稳定性好，使用寿命长等特点。工作电压为5V，具有DO输出针和AO输出针，AO输出针为模拟输出，本项目采用AO管脚，用于实现模拟输出。
从电路图左边开始分析，上图中QM-N10就把它当成一个可变化的电阻即可。当检测到气体浓度时，QM-N10电阻开始变小，由分压公式可得到图中电压比较器U1A的2脚电压为VCC*R2/(Rqm-n10+R2)；由于Rqm-n10变小，U1A的2脚电压变大，当该电压大于U1A的3脚电压时（值为2.5V，因为该参考电压由滑动变阻器Rp调节），此时输出低电平0。此时VCC经过R3限流电阻经过LED，会使得LED灯亮，代表检测到了一次气体浓度，然后输出到DO引脚。但本项目中，未使用到DO脚，因此，你会观察到LED灯亮，却不会在MCU上接收到低电平0。上图中AO引脚直接与U1A的2脚相连接，因此在本项目中，只需要将AO脚连接到MCU的ADC接口，根据公式即可计算出气体浓度值。

￼

￼

### 6.4 按键模块电路分析

￼

￼![image-20260611225938694](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611225938694.png)
￼

￼按键模块电路图如上图所示，为低电平触发。在本项目中使用了两个按键，下边对其进行分析。首先该按键模块的工作电压为3.3 V，R7、R8为上拉电阻，目的是维持一个确定的初始状态电平输入到MCU。这里再解释的深一点，我发现好多粉丝朋友不清楚上拉电阻的作用，也不知道为什么某个IO口要设置为上拉输入。上拉或者下拉电阻的核心作用是给引脚一个确定的电平状态，防止引脚悬空（即没有外部信号驱动时）因外界电磁干扰导致电平不确定，从而引发逻辑误判。比如在使用外部中断的时候，如果没有一个确定的初始电平状态，那中断就会由于外界的电磁波干扰从而自行触发。
同理，由于该按键模块需要低电平触发，那防止误触发的情形，在初始状态下，先给一个确定的高电平来让按键保持空闲，因此在编写代码时，就需要将其设置为上拉输入/或者是浮空输入（在浮空输入下，电路状态由外部电路决定）。当按键按下时，低电平由GND流向IO口，输入到MCU使用。

#### 6.5 LCD电路分析

![image-20260611230032338](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230032338.png)

LCD电路如图6.7所示，主要分为几个模块。液晶屏是最主要的，它是显示信息的直接模块，其底层由背光限流电路和电平转换电路进行连接。背光限流电路可以看到，由3.3 V电压进行驱动，R9为限流电阻，防止电流过大烧毁液晶屏。然后经过R9电阻后输入到LED端口，这个LED端口连接到上图的液晶屏。所以说有粉丝问，博主我的LCD屏为什么不亮？那就是背光限流电路没上电，LCD自然亮不起来。
电平转换电路主要是连接SPI接口和液晶屏，用于将MCU输出的信息显示到液晶屏上。最后，分析一下右边的电源电路。该电源电路主要是用于电源选择，能兼容3.3 V/5 V电源。当你接入5 V电源时，经过图中的U4降压电路，将5 V电源转换为3.3 V电源输出，此时J1断开。当接入3.3 V电源时，此时J1短接，直接输出3.3 V，C1电容用于3.3 V电源滤波，输出稳定的电压。

#### 6.6 蜂鸣器电路分析

![image-20260611230056280](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230056280.png)

蜂鸣器电路如上图所示，从左往右分析，当PB15输出高电平时，经过限流电阻R10（限流电阻的作用就是限流，防止电流过大烧毁三极管）输入到NPN三极管。此时NPN三极管导通，低电平从GND流向蜂鸣器的负极，此时蜂鸣器发声；当PB15输出低电平时，此时NPN三极管截止，蜂鸣器的负极断开，没接到地，就不会工作，不会发声。R11是下拉电阻，用于给NPN三极管一个确定的低电平初始状态，防止三极管误开启，因为该三极管是高电平导通。该项目的电路分析主要分析以上关键模块，如果有对STM32F103C8T6系统板电路分析感兴趣的，请查看笔者小红书电路原理图讲解合集。

￼

￼
7. ## 软件原理实现

  ￼

  #### 7.1 rtos移植与内核裁剪

  ￼
  常有粉丝问我，rtos的移植和内核裁剪到底是什么东西？感觉一直不是很明白。因此在开始讲解源码之前，先来说一下底层的一些东西。实际上，rtos的移植与其他源码的移植是差不多的。以Keil为例，就是先从官网上下载rtos的源码到本地某个文件夹，然后移植到Keil中，添加好.c文件和对应的.h文件即可，只不过多了一些文件的修改操作，而这些文件修改操作却是rtos与STM32的硬件架构（特别是内核、定时器和中断）完美适配的关键。
  因此本章节重点是阐述rtos移植过程中需要修改哪些文件，至于详细的移植过程，请查阅学习资料文件夹--STM32F1 FreeRTOS开发手册_V1.1.pdf。

  #### 7.1.1 时间管理文件port.c的修改

  ￼在该文件中，主要关注的是xPortSysTickHandler()函数。其主要用于处理SysTick中断，负责递减每个任务的时钟节拍计数器，并在需要时触发PendSV进行任务切换。如果rtos没有这个时钟处理函数，那么延时函数也不会发生任务调度。而在STM32中，采用的是24位的倒计数Systick定时器来作为rtos的时钟；因此，修改点在于：需要在port.c文件里编写Systick定时器的初始化函数，开启Systick中断，并且需要在Systick中断处理函数中调用xPortSysTickHandler()函数。
  而在本项目中，笔者为了模块化管理，将与rtos时钟有关的函数全部写在delay.c文件里边，并且将rtos的延时函数用delay_ms做了简单封装，因此并未在port.c文件里进行相应修改。
  如果你自己更想在port.c文件里实现rtos的时间管理功能，那也是可以的。此外，有关于delay.c文件的源码详解，请看笔者的博文：FreeRTOS移植中时钟管理文件的修改_stm32移植freertos滴答定时器-CSDN博客.￼

  ### 7.1.2 FreeRTOSConfig.h文件的修改

  该文件称为内核裁剪文件，实际上内核裁剪操作，指的就是对该文件进行添加以及修改。该文件具有极高的修改自由度，需要针对所做的项目以及所选用的MCU硬件做特定的修改。如在本项目中，笔者已经对该文件进行了特定的修改，如果此时将STM32F1换成F4，或者是对任务的堆栈大小超过笔者设定的值，那么rtos是跑不起来的。因此，该文件具有专业定制性。说完该文件的特点之后，下边说一下内核裁剪的工作原理：本质上内核裁剪就是利用条件编译，打开或者关闭对应的宏，以达到开启/关闭对应内核功能的目的；而内核配置本质上是根据选用的MCU硬件参数做出相应的配置，如主频配置、中断配置等。
  了解原理后，来看一下具体怎么修改的。在FreeRTOS的官方例程中，每个工程都有一个FreeRTOSConfig.h文件。因此在移植rtos时都是参考对应MCU的配置文件，再进行相应的修改或者添加。先来看FreeRTOS基础配置选项的修改，如图7.1所示：

￼![image-20260611230209987](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230209987.png)
￼

图7.1
如上图所示，一般而言，高亮处就是需要修改的地方。第一个是是否启用低功耗的宏，本项目中未用到该模式，因此关闭为0；第二个是CPU的频率，由于笔者使用的是STM32F103，因此时钟频率为72 MHz（用宏SystemCoreClock增强可读性）。如果你使用的是STM32F4，那此处就需要修改为168 MHz了。第三，时钟节拍频率，rtos中都有一个时钟节拍，一般默认为1000 Hz，即周期为1 ms中断一次，因此不用修改；第四，可使用的最大优先级，这里由于笔者使用的MCU是32位的，加上使用硬件方法（计算前导零的个数）来寻找下一个最高优先级的任务，这种方法能快速获取优先级最高的就绪态任务，但是会限制任务的优先级个数，因此STM32只能有32个优先级，此处只能填32。
解释一下如何通过计算前导零个数来找到优先级最高的就绪态任务。使用每个bit代表一个优先级，bit0代表优先级0，bit31就代表优先级31，当某个优先级有就绪任务的话就将其对应的bit置1。前导零个数就是指从最高位开始(bit31)到第一个为1的bit，其间0的个数。如二进制数1000 0000 0000 0000的前导零个数就为0。二进制数0000 1001 1111 0001的前导零个数就是4。得到前导零个数以后用31减去这个前导零个数得到的就是处于就绪态的最高优先级，举例：比如优先级30为处于就绪态的最高优先级，30的前导零个数为1，那么31-1=30，得到处于就绪态的最高优先级为30。

下边再看一下与内存有关的配置项修改。如图7.2所示：￼

![image-20260611230306783](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230306783.png)

由于FreeRTOS支持两种内存分配方法：动态分配和静态分配。动态分配指的是分配堆内存、静态分配指的是分配的静态区内存（如果你想使用静态分配方法，需要在RAM上自定义内存，即自定义全局数组，数组大小就是任务栈大小）。从上图可以看到，在本项目中，笔者使用的是动态内存分配的方法，因此对应的宏要置为1。
并且，需要定义好系统分配堆的总大小，此处笔者定义为10 Kb。因为FreeRTOS在创建任务、信号量、队列时会使用heap_x.c(x为1~5)中的内存申请函数来申请内存（一般选择heap4）。而内存就是从堆ucHeap[configTOTAL_HEAP_SIZE]中申请的。因此这个总的堆大小你需要根据你所创建的任务的堆栈大小来进行合理定义，比如笔者项目中所有任务加起来的堆大小并没有超过10 Kb。需要注意的是：如果你定义的堆size过小，会导致堆栈溢出，从而进入HardFault；如果定义的堆size过大，则会导致超过MCU的RAM大小（因为堆区、栈区、全局区的数据都保存在RAM里边，而code区和只读区保存在flash中），导致无法编译。
在图7.2的下半部分是一些钩子函数的配置。钩子函数一般用于调试打印，本项目中并未使用钩子函数，因此，此处不开启对应的宏。如果你想要使用钩子函数，需要自行实现函数功能。
最后看一下与中断有关的配置项修改，如图7.3所示：

![image-20260611230340729](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230340729.png)

从上图可以看到，第一个是FreeRTOS的中断配置宏configPRIO_BITS的修改。该宏用于设置你所使用的MCU支持的中断优先级位数，在本项目中，笔者使用的是STM32F103，这款MCU的中断优先级位数被指定为4位（这是半导体厂商决定的），因此此处需要将宏的值指定为4。
下边是宏configLIBRARY_LOWEST_INTERRUPT_PRIORITY的修改。该宏用于指定中断的最低优先级。由于FreeRTOS并不支持亚优先级管理（STM32的优先级配置寄存器是8位宽，前4位用于抢占优先级，后4位用于亚优先级），因此中断配置组NVIC只能设置为组4，即4位抢占优先级，0位亚优先级。所以中断最低优先级为15，此处宏的值应该修改为15。
接下来是宏configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY的修改。此宏用来设置FreeRTOS系统可管理的最大优先级，此处笔者设置该宏的值为3，代表的意思是高于3这个优先级的中断就不归属FreeRTOS管，高于优先级3的中断处理函数里边，不能调用FreeRTOS的API函数，也不能被FreeRTOS所屏蔽。所以笔者在本项目中所设置的中断优先级都低于3，如果高于3，会触发HardFault。
如下图所示的两行配置宏是默认的，一般不用修改。

![image-20260611230414485](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230414485.png)

简单说一下其含义。第一行宏configKERNEL_INTERRUPT_PRIORITY是用来设置内核中断的优先级，也就是设置Systick中断优先级以及PendSV中断优先级。可以看到此处将最低优先级15左移4位后赋给宏（内核中断优先级），代表意思是将Systick中断以及PendSV中断优先级设置为最低，以实现延缓上下文切换，保证普通中断能及时响应。为什么要左移4位呢？因为前边说过了，FreeRTOS中仅使用高4位（抢占优先级），因此要高4位才是真正的中断，所以要左移4位，而不是用低4位。而第二行宏也是管理最大优先级的，表示低于此优先级的中断可以安全的调用FreeRTOS的API函数，高于此优先级的中断FreeRTOS是不能禁止的，中断服务函数也不能调用FreeRTOS的API函数！
最后，说一下图7.4的配置修改：

![image-20260611230428603](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230428603.png)

首先，这两行宏定义在官方给的FreeRTOSConfig.h文件里是没有的，这是后续添加进去的，那为什么要定义这两行宏呢？如果大家阅读过FreeRTOS的源码，就知道PendSV中断是真正的任务却换函数，而SVC中断是用于启动第一个任务的。而在FreeRTOS中，这两个函数分别被定义成了xPortPendSVHandler和vPortSVCHandler。
那为什么我们这里非要这样多此一举呢？这是因为要与启动文件保持一致性。下边仔细分析：在CM3架构中，中断响应是通过中断向量表来实现的。每发生一次中断，都会去CM3内部找到这张表，再从表上取出对应的中断处理函数入口，从而跳转到中断处理函数执行。而这张中断向量表存在于启动文件中，如图7.5所示：

![image-20260611230458844](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230458844.png)

从上图可以看出，当发生中断时，就从该向量表中找到对应的中断处理函数入口地址，跳转执行。比如说发生了PendSV异常，那么NVIC就会计算偏移量是0x38（如对内核架构不清晰，请查看学习资料文件夹--CM3权威指南），从此处取出服务例程的入口地址，跳转执行。如启动文件下图7.6所示：

![image-20260611230517437](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230517437.png)

上图是由汇编代码编写的，第一行PendSV_Handler代表函数名，下边的是函数体。WEAK指的是弱引用，即当其他地方定义了同名函数，就会覆盖此函数。比如我在外部文件里边定义了PendSV_Handler函数，就会执行外部文件的同名函数，不会执行启动文件的。
因此，FreeRTOS文件里边已经定义了该函数为xPortPendSVHandler，而我们仅需要将xPortPendSVHandler函数重定义为PendSV_Handler（即#define xPortPendSVHandler PendSV_Handler），这样就能与启动文件的中断向量表联系起来了，才能正常的去响应PendSV中断。当然，这里也不一定需要添加这一行宏定义。你也可以选择将启动文件的对应函数名修改成xPortPendSVHandler，但这样风险较高，启动文件是官方给的文件，我们能不修改则不修改，否则极易出错。

#### 7.1.3 stm32f10x_it.c文件的修改

￼
最后，我们还需要修改此文件，修改也很简单，只需要将如图7.7所示的几个函数给注释掉即可：因为我们已经在FreeRTOS文件中实现了相应的函数功能，如果不注释掉，编译的时候会报错：重复定义！

![image-20260611230730401](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230730401.png)

### 7.2 应用层源码详解

￼
明白rtos的移植和内核裁剪原理之后，接下来正式进入项目的源码讲解。在本章节，笔者将从应用层到驱动层，以顶部往底层深挖的顺序来讲解源码。下边先来理清楚应用层源码的执行流程。
不管是对于本项目还是之后的工作，大家拿到项目后，都应该先从main函数入手，这是代码的原点。接下来看一下main函数都干了些什么。如图7.8所示：

![image-20260611230754354](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230754354.png)

Main函数很简单，先进行硬件的初始化操作，再完成一些数据的初始化、接下来创建开始任务、最后开启多任务调度，开始运行第一个任务。这里也引入了FreeRTOS的启动流程，这也是一个高频面试点（后边高频面试点章节不再重复叙述）。
启动流程为：当上电复位后，内核从0x08000000处取出堆栈指针（根据启动方式不同，地址不同，一般在Flash启动），完成堆栈初始化；接下来在0X08000004 处取出复位中断的入口地址加载进PC寄存器，此时跳转到复位中断函数执行，在该函数里先进行系统时钟的初始化，然后调用__main函数将Flash里边存储的data数据拷贝到SRAM上，将BSS段变量进行清零。最后，调用main函数执行。在main函数中先完成必要的硬件初始化，然后创建开始任务完成RTOS的初始化，这包括：创建开始任务、TCB的初始化及任务堆栈初始化，将任务加入就绪表等操作。
最后一步就是开启多任务调度，该函数会创建内核必需的空闲任务，若启用软件定时器则同时创建定时器服务任务。随后调度器被使能，并调用硬件移植层接口xPortStartScheduler，该接口配置PendSV和SysTick异常的优先级，启动系统滴答定时器，并通过触发SVC异常来激活首个任务。
在SVC异常服务程序中，系统从第一个任务的任务控制块中恢复其上下文，包括堆栈指针和程序状态字。当异常返回时，CPU跳转到任务入口函数，至此系统正式进入多任务调度环境。后续所有任务由内核基于优先级和事件进行抢占式调度，而vTaskStartScheduler函数将永不返回。

#### 7.2.1 System_Init函数

￼
关于硬件初始化函数主要是对各个硬件进行初始化操作，具体的细节笔者将会在驱动层源码详解部分进行讲解，因此本小节先略过，接下来先看一下system_init函数。如图7.9所示，该函数首先是对系统状态结构体的数据进行初始化操作，然后创建互斥量、信号量。
这里解释一下为什么要用互斥量？首先要理解什么是互斥量，互斥量就是一次只能由一个任务能访问共享资源。就像公共电话亭，一次只能有一个人进去打电话，等别人打完电话后，你才能进去使用。同理，在本项目中有一个全局系统状态结构体g_systemState，需要被多个任务同时访问。如果不使用互斥量的话，会导致结构体成员可能在写入一半时被其他任务抢占，导致读取到不一致的数据。因此，使用互斥量是为了保护共享数据g_systemState，防止多个任务同时读写造成数据混乱（悄悄给你们讲，这又是一个高频考点）。

![image-20260611230823585](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230823585.png)

而创建二值信号量，是为了中断同步，因为在中断函数里业务逻辑写的过于复杂是大忌！中断函数应该快进快出，因此采用信号量将复杂的逻辑业务剔除。

#### 7.2.2 StartTask

￼
现在请把自己当成一个PC寄存器，看一下程序到底是怎么跑的？完成了初始化，并创建好第一个任务之后，开始启动任务调度。由于此时任务就绪表里边仅有唯一任务：StartTask，因此cpu会首先运行该任务。
如图7.10所示，该任务首先进入临界区，防止被中断所打断。临界区函数的底层原理就是操控BASEPRI寄存器，屏蔽中断；然后开始创建任务，创建完任务之后，再去执行TIM4的定时器中断初始换函数。那么问题来了，作者为什么要在此处进行TIM4的中断初始化，而不是在一开始就进行初始化呢？我帮你们问了一下作者：如果一开始就进行TIM4中断的初始化，那初始化结束之后，会立马触发TIM4中断，但是TIM4中断由于调用了FreeRTOS的API函数，而此时程序还未创建好对应的FreeRTOS信号量；那此时FreeRTOS的API里边的参数就是一个野指针，那么就会触发HardFault，导致程序死机。如果你非要在一开始（硬件初始化函数中）就初始化TIM4中断，那你需要在其驱动函数中先关闭TIM4中断使能，后边再打开。

![image-20260611230858335](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230858335.png)

等初始化完TIM4中断之后，开始退出临界区，此时StartTask将屏蔽的中断打开，然后删除自身，代表将自己在任务就绪表中拉黑。

#### 7.2.3 先运行TIM4中断/SpeedCalcTask任务

￼
由于在StartTask中创建了各个任务，将各个任务加入了任务就绪表中。那此时就会根据任务优先级的高低，开始并发执行各个任务。但这里大家要注意两点：1、并发与并行的区别（并发是指先吃完饭再接电话，并行指的是边吃饭边接电话）；2、要记住中断比任何RTOS任务的优先级都高，只要发生中断，一定会先去执行中断。即使中断程序优先级比应用层任务优先级低，但这仅体现在数值上，因为中断是由硬件触发的。
如上所述，在创建好开始任务之后，中断已初始化完毕、任务创建完毕。此时会先去运行TIM4中断处理函数（图7.11），然后此时发送二值信号量，将原本处于阻塞态的速度计算任务SpeedCalcTask唤醒（图7.12）。需要注意的是速度计算任务的优先级要设置的较高，因为中断发生以后会执行任务切换，该过程要寻找优先级最高的就绪态任务，如果此时速度计算任务优先级不高，将会被其他任务所抢占，无法达到中断同步的目的。

![image-20260611230919168](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230919168.png)

![image-20260611230926157](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230926157.png)

图7.12中采用的是编码器测速，先获取编码器的计数值，然后每50 ms计算一次电机转速，代表其采样精度是50ms（因为TIM4是1 ms中断，计数到50次为50 ms）。但此时电机还未启动，因此测出的速度应为0 PRM。等到再次执行xSemaphoreTake函数时（死循环内），此时速度计算任务由于没收到信号量就会发生阻塞，就会将该任务加入到任务等待表，然后CPU重新开始调度，寻找任务就绪表中其他高优先级的任务执行。
可能由粉丝会问，此处TIM4中断是1 ms，而rtos的时钟Systick中断也是1 ms，这两者之间是否会发生影响呢？是否会影响RTOS的时钟节拍？
答案是不会影响，因为TIM4中断优先级高，会先执行TIM4中断，然后再去执行Systick中断。同时由于TIM4中断函数简短，中断处理在us级，不影响Systick中断处理。
此外，再解释一下笔者为什么使用互斥量加全局变量而不使用队列来进行任务间通信？因为本项目中的数据模型需要共享最新状态，而不是历史数据；需要多读多写，关注的是最新值，用互斥量最为合适。且用互斥量加全局变量内存开销比较小，仅需一个结构体；而使用队列内存开销大，需要定义多个队列缓冲区。

￼

￼

#### 7.2.4 再运行MotorControlTask

￼
如图7.13所示，任务优先级及对应的堆栈大小如下（app_task.h）：

![image-20260611230953506](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611230953506.png)

最高优先级的是IAP任务（FreeRTOS中数值越大，优先级越高），但由于IAP任务是由DMA完成中断使能二值信号量触发的，因此在DMA完成中断触发之前，IAP任务处于阻塞态，存放在任务等待表中，而非任务就绪表中，则此时任务就绪表中最高优先级的任务为MotorControlTask，则当速度计算任务处于阻塞态之后，MotorControlTask开始运行。
MotroControlTask源码如图7.14所示：

￼
￼![image-20260611231006002](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231006002.png)

从上图可以看到该任务需要等待一个互斥量g_dataMutex，该互斥量由下图7.15所示的各任务共同访问：

￼
￼![image-20260611231018117](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231018117.png)

而在上图中的各任务中，此时先运行的是MotorControlTask。但现在图7.14的源码中需要先等待一个互斥量，而现在图7.15中的其他任务都没有执行，那此时这个互斥量应该由谁来释放呢？如果这个互斥量没有任何任务释放，那MotorControlTask也不会运行。答案是创建互斥量函数xSemaphoreCreateMutex。
在该函数的源码中（图7.16），会释放一次互斥量，使其初始状态可用。即当运行到图7.14所示代码时，此时会直接通过条件判定if (xSemaphoreTake(g_dataMutex, portMAX_DELAY) == pdTRUE)。

![image-20260611231038662](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231038662.png)

回到图7.14源码继续分析（建议不想往上翻的一边看工程代码，一边分屏看文档），当判断互斥量等待成功后，将编码器所测的转速赋给系统结构体的actualPRM成员，然后释放互斥量。我们知道，调用释放互斥量函数时，会执行一次任务切换（不熟悉的请自行看FreeRTOS源码，本文档不对RTOS源码做详细分析，有需要的可以看笔者的小红书--RTOS入门学习合集，或者学习资料文件夹--STM32F1 FreeRTOS开发手册_V1.1.pdf），任务切换过程主要做两件事情：1、寻找优先级最高的就绪态任务；2、执行上下文切换。但此时系统中最高优先级的任务就是当前任务MotorControlTask，因此，此时并不会发生任务切换，程序继续往下执行。
如图7.17源码所示：

![image-20260611231053697](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231053697.png)

此时程序开始执行状态机业务逻辑。为什么需要状态机呢？这是因为在本项目功能需求中，有手动模式、自动模式、防回流模式以及待机模式（见5.3.2章节）；而每一种模式在同一时间只能单独存在，因此采用了状态机思想进行编写。
先来看待机模式，待机模式下先调用函数motor_stop保证电机处于停止状态，然后给系统结构体g_systemState的成员motorRunning（0代表电机停止）、autoModeCounter（自动模式计数器）、cookingEventCounter（cooking模式计数器）赋值，最后设置状态为待机。Note：工作模式的切换通过按键扫描任务来完成，后续进行分析。
下边看手动模式的业务逻辑（请结合5.3.2章节功能需求看代码）。首先，当切换到手动模式下，如果此时的电机处于关闭状态，需要开启电机，等待电机开启后，根据设置的目标转速，通过PID算法进行输出占空比。
接下来看自动模式的状态机业务逻辑。根据5.3.1章节的功能需求分析，自动模式下分为以下几个阶段：等待阶段（等待cooking event）、cooking阶段、delay_off阶段（cooking_event结束后，需要一段时间慢慢关闭电机）。因此在自动模式下，又可以分成三个状态机逻辑（这就是状态机嵌套）。等待阶段状态机源码如图7.18所示：

![image-20260611231116111](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231116111.png)

从上图可以看到，在启动阶段，电机需要以最小转速开始运行，对应的最小占空比为20%。然后自动模式计数器autoModeCounter自增50，这里自增50是因为该任务有50 ms的延时，即运行一次该任务的周期为50 ms，因此，每运行一次该任务，自动模式计数器加50。然后判断cooking_event是否触发，如果触发，就将状态机状态置为cooking阶段；否则判断自动模式计数器是否超时（1 min），如果超时，就从自动模式的启动阶段变为待机模式，并且电机运行状态为0。
启动阶段业务逻辑源码如图7.19所示，当触发cooking_event时，进入该阶段。首先，根据风速算法，输出对应的PWM占空比，此时电机的转速由风速决定。

￼![image-20260611231131433](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231131433.png)
￼然后，cooking模式计数器自增50，接下来判断cooking_event是否结束；如果结束，切换到delay_off阶段，否则判断cooking阶段是否超时（1 min），超时的话就切换到待机模式，电机停止。
Delay_off阶段业务逻辑源码如图7.20所示：

![image-20260611231143950](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231143950.png)

根据上图，在delay_off阶段，电机转速依然由风速算法决定。然后判断是否再次触发cooking_event，如果触发，跳转到cooking阶段；否则看delay_off阶段是否超时（10 s），如果超时，则跳到待机模式，电机停止。
最后，来看一下防回流模式业务逻辑源码（坚持住，这个比任务终于要分析完了），如图7.21所示：

![image-20260611231203237](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231203237.png)

防回流模式是为防止楼道邻居的气味回流而设计，由防回流任务所控制，当且仅当防回流模式被激活，且电机运行时，进入上图if分支执行。函数体内部依然使用用户设定的挡位结合PID算法来执行电机转动。接下来程序调用delay_ms(50)，此时MotorControlTask任务开始主动放弃CPU控制权，进行任务切换，寻找下一个优先级高的就绪态任务—按键扫描任务执行。

#### 7.2.5 再运行KeyScanTaskx'x'x'x'x's

￼
该任务的业务逻辑源码如图7.22所示。由于该任务源码较简单，笔者就不对上图代码进行逐行分析了。主要说一下两个按键的业务拆分。其中，KEY1用于工作模式的切换，包括待机、自动、手动、防回流。而KEY2分为两种情况，一种是短按，此时用于切换电机转速的挡位（low/high）；另一种是长按，用于强制开关机，停止/启动电机转动。此外，还可以看到，该按键扫描任务采用的是轮询读取按键状态的方式来编写的代码，而不是采用中断的方式来编写代码，那为什么要这样写代码呢？
答案是如果采用中断的方法，会导致消抖处理比较麻烦，因为在不采用延时消抖的情况下，需要引入定时器去进行消抖处理，增加了业务复杂度；而采用轮询读取按键状态的方法，仅需要用状态机消抖即可，且采用10 ms的周期进行轮询、加上该任务优先级较高，因此能保证按键响应及时。

![image-20260611231228740](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231228740.png)

#### 7.2.6 再运行SensorTask

￼
等待按键扫描任务执行到delay_ms(10)这一行代码，意味着此时CPU又需要重新找其他的高优先级就绪态任务执行了，此时就会找到优先级为3的SensorTask以及WindSpeedTask。那先执行哪个呢？答案是随便，由于这两个任务优先级相同，因此FreeRTOS采用基于时间片轮转调度，等待其中一个任务时间片用完，再切换到另一个任务。因此，笔者就假设先运行SensorTask，先讲解该任务的业务逻辑源码。
如图7.23所示：

![image-20260611231253613](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231253613.png)

可以看到，上图中的代码还是比较易懂的，就是先后读取了DHT11和MQ2传感器的数据。其中，需要多说一点的是，这里为什么等待了两次互斥量呢？笔者在之前说过，使用互斥量是为了防止多任务竞争导致全局变量读写混乱。此处编写的代码执行了等待两次互斥量操作，看起来好像是多此一举，但实际上这是为了程序运行的稳定性考虑。因为如果此处仅执行一次等待互斥量操作，可能会导致该任务占用资源时间过长，更容易带来优先级反转问题，因此采用等待两次互斥量的操作来减少资源占用时间。此外，代码中有一个条件编译宏SENSOR_DEBUG（基本每个公司的代码规范都要求宏的全称要大写！因此建议大家编写代码时就养成好习惯），该宏是笔者用于输出调试信息的，如果你想要看到串口的打印输出，需要开启此宏。

#### 7.2.7 再运行WindSpeedTask

￼
该任务的源码如图7.24所示，首先等待到互斥量资源后，就去获取传感器数据，然后调用风速算法函数计算风速（风速算法在后边会说到）；最后更新一下系统状态，包括当前的风速值及cooking_event使能标志，最后依然调用延时函数放弃CPU控制权。

![image-20260611231315720](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231315720.png)

#### 7.2.8 再运行AntiBackFlowTask（动态阈值切换算法）

￼
由于之前的任务都放弃了CPU控制权，接下来开始运行防回流任务。其业务逻辑源码如图7.25所示：

![image-20260611231338572](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231338572.png)

从上图可以看到，首先判断当前工作模式是否处于防回流模式下，如果不是，重置为初始状态；如果处于防回流模式下，判断当前检测到的气体浓度是否超过设定的低阈值。超过了低级阈值，则将检测标志置1，并且继续判断当前的气体浓度是否超过高阈值，如果没超过，则开启电机并切换气体阈值为高。如果超过，则什么也不做。最后，如果检测到气体浓度低于低阈值，则关闭电机，气体浓度阈值切换为低。
那问题来了，为什么要这么写呢？答案是根据功能需求，为了防止电机检测到气体浓度超过阈值后，启动之后又多次发生重启的情况，因此需要设置动态阈值切换算法，如图7.26解释：

![image-20260611231353758](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231353758.png)

从上图可以看到，电机仅在高于低阈值时启动，在低于低阈值时关闭；如果在极端情况下，检测到的气体浓度高于高阈值，此时由于电机已经启动过，保持电机转动即可，不需要任何操作。注意：气体浓度的上升不具有突变性，因此不会在初始状态下就超过高阈值。
由于此处有多位粉丝朋友在问，因此笔者再详细解释一下：上述动态阈值切换存在的意义是什么？意义是因为传感器会在短时间内反复检测气体浓度。根据函数，1s会执行很多个循环，会在函数内部运行很多次。那这样就会出现一个问题：假设该函数1us轮询一次，比如我在1us的时候，检测到气体浓度超过阈值，此时已打开风机。但在第2us的时候还是检测到气体浓度超过阈值，由于之前风机已经打开了，此时就不需要重新打开，所以将阈值切换为高，防止再次调用风机启动函数。否则，会在接下来的每一us，都会调用一次风机启动函数，就会造成风机反复启动。注意，风机重启的意思是：在已经启动的情况下，还要再重启一次。通俗点讲就是在打开的情况下，还要再次打开；而不是关闭，打开，关闭，打开。

#### 7.2.9 最后运行UIDisplayTask

￼
该任务由于优先级最低，因此最后运行，其源码如图7.27所示：

![image-20260611231412272](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231412272.png)

从上图可以看到，该任务主要显示工作模式、挡位、风速、电机转速、自动模式状态、自动模式计数器以及cooking阶段计数器在LCD屏幕上，刷新周期为200 ms。
应用层任务的执行流程就讲解到这里，需要注意的是：关于各个任务的栈大小（单位是字，不是字节，1个字为4个字节），这是自定义的。我们知道，栈区主要存放的是局部变量、形参、以及通用寄存器，因此，大家在定义任务栈的大小时，需要根据任务里是否存在多层函数嵌套（如果函数嵌套过多，也容易造成栈溢出！）、是否存在大型数据类型如float、double等、局部变量是否定义过多，从而去灵活的定义任务栈大小，原则是能接受任务栈大一点，但是不能接受任务栈过小导致堆栈溢出！一般是从64个字开始分配栈大小。（这里又是一个可能的高频考点，大家要注意哦）。

### 7.3 固件升级功能详解

￼
这一章节也是粉丝问题最多的地方。在之前推出的项目中，有很多粉丝私信我，到底什么是bootloader？什么是APP？是手机APP那种吗？有什么用？解决什么问题等等。因此，笔者希望在本章节把这些问题讲透讲懂。当然，个人希望大家在阅读本章节前，能够先查阅一下相关知识，能加快你理解本章节—请查看学习资料文件夹—STM32F1开发指南-库函数版本_V3.3.pdf—第52章IAP实验，以及笔者的CSDN博文：https://blog.csdn.net/weixin_52247452/article/details/139626409?spm=1001.2014.3001.5502

#### 7.3.1 为什么要用固件升级功能？

￼
我尽量一句话说清楚：目的就是为了产品发布以后想要对产品中的固件程序进行更新，那就需要重新烧录程序，但是产品一般都封装完好，不能拆开进行烧录调试，此时就需要IAP通过预留的通信接口（包括但不仅限于串口、IIC、SPI、CAN等通信方式），将所要更新的程序传入设备，以达到快速更新程序。比如汽车的OTA更新，如果不用OTA更新，只能去线下把汽车拆开再烧程序了，非常麻烦。其中，IAP全称是In Application Programing，即在程序中编程，意思就是说把需要更新的程序传入原来的程序中，原来的程序接收到了更新程序，就跳转到更新程序去执行。

#### 7.3.2 什么是bootloader和APP程序（高频考点）？

￼
Bootloader叫做引导加载程序。在嵌入式系统上电复位后首先运行引导加载程序，它的功能主要是负责系统的上电自检、必要的硬件初始化、建立储存空间映射，并加载和启动操作系统。Bootloader一般储存在bootROM中，当前使用最多的类型是NOR flash rom，在大多数的嵌入式系统中，flash里边不仅储存了bootloader，还储存了用户程序代码。Bootloader有两种工作模式，一种是启动加载模式，另一种是下载模式；而本章节所提到的固件升级就属于bootloader的下载模式。注意：在本项目中，整个烟机项目就是一个巨大的bootloader程序（本文后续出现的bootloader指的就是烟机程序），因为它能接收固件并进行跳转执行。那为什么不做一个只有更新功能的bootloader，非要在bootloader里边实现如此多的功能呢？因为在公司里一般都是这么用的，固件更新功能只是为了以防万一，有需要更新的时候才进行更新，不需要更新的时候用原来的功能就够了。
接下来说一下什么是APP程序。APP指的就是需要传输的更新固件，不是手机APP，通常以.bin文件形式发送。又有粉丝问，那为什么要用bin文件，而不用hex文件呢？因为hex文件是包含地址信息的ASCII文本文件，可直接用于烧录；bin文件是纯二进制数据文件，不含地址信息，烧录时需要指定起始地址。刚好APP区域的划分是由我们自行划分的，起始地址只能自己指定，APP程序自然是要烧录到APP区域执行，因此需要未指定起始地址的bin文件。

#### 7.3.3 固件升级的工作原理是什么？

￼
固件升级的工作原理实际上就是：先将整个Flash划分成boot区和APP区，boot区专门用于执行bootloader，APP区专门执行APP程序；然后将bootloader烧录到boot区，接下来通过上位机传入固件，此时bootloader接收固件并跳转到APP区域执行固件。
上述这段话是一个总结，接下来从STM32的上电启动流程、bootloader到底干了什么以及APP程序如何跳转三个层面来进行具体分析。
（1）STM32上电启动流程
（以下是高频考点）当STM32发生复位时，此时硬件会强制PC寄存器指向一个固定地址0X00000000（或者是由boot引脚映射的0x08000000），该地址存放的是主堆栈指针（MSP）的初始值，内核会读取该值并进行堆栈初始化，以创建好C环境以及安全调用中断服务函数。
其次，PC寄存器会执行到0x00000004地址，取出该地址存放的复位中断处理函数，并跳转过去执行；在复位函数中，首先完成系统时钟的初始化，其次调用__main函数，完成数据的初始化，如将Flash中的data段数据拷贝到RAM中，然后将未初始化的全局变量（bss段）清零。最后，__main函数调用main函数，进入到main函数执行。
（2）bootloader程序在固件升级中到底干了什么？
一句话说清楚这个问题。实际上，在固件升级功能中，bootloader就干了三件事情：1、接收上位机传输的固件保存到RAM中；2、将RAM上的固件拷贝到Flash；3、跳转到APP执行。
（3）APP程序怎么跳转的？
跳转APP程序操作实际上就一行代码--调用jump2app()。那为什么调用这个函数就行了呢？答案是jump2app是一个函数指针，直白点，它就是一个地址0x00000004+M(M是偏移量，或者说是boot区与APP区的分界线)；这个地址看起来很熟悉对吧？因为才在上电启动流程里边见过，不同的是多了一个M，这代表的意思是在APP区里重新创建了一个新的中断向量表（依然0x00000000+M存放堆栈指针MSP、0x00000004+M存放复位向量）。
理解了这行地址代表的含义之后，我们再来看jump2app()的含义。这实际上就是取地址操作（这里我不多解释了，如果你不明白，说明你C语言基础还要补），而这个地址里存放的是APP区的复位向量。因此，此处代表的意思就是取出复位向量并跳转执行，也就是执行到了APP区的复位中断服务函数中，然后再次执行系统时钟的初始化、调用__main函数初始化数据，最后调用APP的main函数执行。到此处，开始执行APP程序的main函数功能，跳转APP程序执行成功。

￼

￼

#### 7.3.4 不加CRC32校验的固件升级代码详解

￼
（1）串口加DMA接收固件源码详解
通过上述3个小节的原理讲解，相信各位粉丝朋友已经对固件升级有基本认知了。接下来，进入实战检验，看看本项目中固件升级代码到底怎么写的（升级软件框架见5.2章节）。还是按照由浅入深的规矩，别管什么CRC校验，先把固件升级代码肝懂。
如果先把CRC校验功能剔除，那固件升级的流程如图7.28所示：

![image-20260611231522069](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231522069.png)

从上图可以看到，当固件从上位机传到STM32的串口时，需要通过DMA搬运到RAM上保存起来。这一部分对应的源码如图7.29所示：

![image-20260611231538964](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231538964.png)

可以看到上图是DMA完成中断服务程序。难道就这个函数就完成了固件的接收吗？并不是，实际上，接收已经在中断函数之前就处理完了，到触发DMA中断函数进行处理时，说明固件已经接收完成，才能触发DMA中断；否则，中断无法触发。
接下来看是如何接收固件程序并搬移到RAM的（前提是你要熟悉串口以及DMA知识）。首先，本项目中是通过串口调试助手来发送固件到STM32的串口的。因此需要编写完整的串口驱动函数，我仅截取了串口驱动函数的关键源码（图7.30）：

![image-20260611231555332](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231555332.png)

上图中在串口初始化完毕之后，需要使能串口DMA接收功能，表示当串口接收到数据后，会触发DMA操作；并且，需要将串口的接收中断功能关闭，因为不需要。DMA指的是将数据在内存和外设之间进行搬移，而不需要CPU的参与。因此，还需要DMA的驱动函数（图7.31）：

￼
￼![image-20260611231607803](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231607803.png)

首先第一行cndtr，表示内存缓冲区的size。因为我们是通过DMA将数据从外设接收到内存，这个内存指的是RAM上的内存；而全局变量、局部变量等就保存在RAM上，换句话说，我定义了一个全局数组就是内存，而数组大小就是此处的cndtr。Cndtr的值如图7.32所示：

![image-20260611231619211](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231619211.png)

内存缓冲区（cndtr）设置成3692是因为APP固件的大小再加上4字节的CRC码。注意：这里的内存缓冲区大小一定要设置的和你的APP固件大小刚刚好，其他情况都不行；如果APP固件小于设置的内存缓冲区，无法触发完成中断、如果APP固件超过设置的内存缓冲区，将造成固件数据截断，传输必然出错进入HardFault。
那问题来了，如何看APP固件大小为多少呢？答案是打开你的APP程序Keil工程，编译完之后，用鼠标双击左侧栏APP文件，召唤出MAP文件，翻到最底下，如图7.33：

![image-20260611231634554](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231634554.png)

上图显示的3688就是固件bin大小，笔者设置的内存缓冲区为3692，是需要加4字节的CRC码。
继续看图7.31的第2行和3行代码分别设置外设的地址和内存的地址。那么外设的地址就应该是串口接收地址；由于USART是全双工的，因此都是通过DR寄存器来收发数据，因此地址为&USART1->DR。而内存地址就是我们设置的全局数组地址receive_buff（数组名代表地址，C语言基础知识）。
第4-7行解释：首先，将数据的传输方向设置为串口到内存，因为我们需要将固件传输到RAM上；其次，设置外设的地址不变，而内存的地址递增。因为固件数据是从外设开始搬移，每次取最新值就可以；而内存的地址如果不从低地址开始递增，那数据就会被覆盖，只会存在新的数据，历史数据就完全找不到了，会导致接收的数据不完整。
第8-10行解释：首先前两行是设置接收的一个数据宽度为8位，因此，我们在定义全局数组时，就要定义为8位类型（图7.34）：

![image-20260611231647251](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231647251.png)

从上图看出，有一个__attribute__，这表示将数组定义在RAM上的某个地址（0x20004000）处，为什么要这样呢？因为我们是需要将整个64KB的flash划分成boot区和APP区；同理，我们也需要将20KB的RAM（起始地址0x20000000）划分成两个区域，以确保bootloader程序的RAM区域与APP程序的RAM区域不重叠，否则会跳转失败。
那新的问题又来了，为什么要偏移0x4000（16KB）才开始接收APP固件程序呢？这是因为我的bootloader程序的MAP文件中的RW SIZE（与图7.33同样的方法可以看）为13KB左右。因此，我取16 KB（需要内存对齐）。还剩下4 KB的RAM刚好够接收APP固件（毕竟大小才3688字节）。
继续解释DMA驱动的第10行代码。此处需要设置为正常模式，而不是循环模式，因为如果是循环模式，当内存缓冲区接收满以后，又从低地址开始接收数据，导致数据被覆盖，无法触发完成中断。剩下的DMA驱动代码不多解释了，自己看吧。

￼

￼
（2）拷贝APP到Flash以及跳转APP源码详解
理解上述串口加DMA接收固件原理之后，把时间拉回到图7.29。接收完固件、触发完DMA完成中断，这个时候就该做拷贝、跳转操作了。
可以看到图7.29中有一个释放信号量操作，那我们就需要去找接收该信号量的任务—IAP任务（图7.35）：

![image-20260611231701665](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231701665.png)

从上图可以看到，当等待到信号量后。首先，去计算接收的固件数据量；其次，判断内存缓冲区数组是否有值，如果有值代表固件数据已通过DMA搬运到RAM上。下边是CRC32校验结果的判定（此处先不关心原理，下节再讲），如果判定结果为0，代表CRC校验失败，需要清空内存缓冲区数组、擦除APP区域，等待下一次重传。只有判定结果成功，才会进入固件拷贝、跳转升级操作。
如图7.36所示，当CRC校验成功后，先计算实际的固件size为接收的固件减去4字节的CRC码。然后取出0x20004000（内存缓冲区数组的起始地址）偏移4字节后的地址上保存的复位向量，并按位与0Xff，提取出高8位，判断这高8位是不是0x08。这样做的目的是：验证接收到的固件是否在Flash有效地址范围内（因为我们是在Flash上升级）。
如果判定接收的固件在Flash有效地址范围内，接下来先进行清屏，并打印正在更新的信息显示在LCD上。接下来先擦除Flash上的APP区域，以防止数据残留。此处需要着重解释下Flash的划分,如图7.37所示：

![image-20260611231716235](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231716235.png)

![image-20260611231723868](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231723868.png)

如图7.37左，当不进行固件升级时，这是一块完整的Flash，大小为64 KB；图7.37右为需要在Flash上进行固件升级的情况，根据7.3.3节一开始讲过，升级需要划分成两个区域，一个为boot区，专门运行bootloader程序；另一个为APP区，专门运行APP程序。可以从上图看到，boot区为60 KB，APP区大小为4 KB，划分的依据是分别查看boot程序和APP程序的MAP文件，以评估各自的Flash大小。
因此，回到图7.36源码FLASH_ErasePage(FLASH_APP1_ADDR)的讲解，现在就能看明白了，这行代码就是在擦除APP区域的Flash（具体的Flash驱动，请大家自行分析，注释已经很清楚了）。
接下来是图7.36源码iap_write_appbin(FLASH_APP1_ADDR,receive_buff,firmwareLen)讲解，该函数主要就是将RAM上保存的固件数据，拷贝到起始地址为0x0800F000的APP区域（Flash）上。然后在LCD上打印更新完成。接下来又是一个是否为Flash有效地址的if语句判断，不同的是这次判定的是APP区域所拷贝过来的数据是否合法，只有合法了，才会进入跳转APP操作。
跳转APP函数源码如图7.38所示：

![image-20260611231740967](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231740967.png)

从上图可以看到，首先需要禁用中断以及相应的外设，防止跳转过程被打断。然后又是一个地址判定，这里的appxaddr参数是之前讲的APP区域起始地址。那为什么这次要判断APP区域的起始地址是否为ram的基地址呢？固件不是已经被拷贝到Flash上了吗？答案是在7.3.3节说过，跳转到APP区域实际上这里还要建立一个新的向量表，只有在新的向量表里边找到新的复位中断函数入口，才会去调用APP的main函数执行。而新的向量表第一行就是堆栈指针，而堆栈都是存在RAM里边的。因此，我们需要验证“这个地址指向的内存里，存放的第一个参数（栈顶指针）是否在合理的物理位置（SRAM内）。
最后，调用MSR_MSP函数对堆栈指针进行初始化，然后调用jump2app函数取出复位中断向量，跳入复位中断服务函数处理，最后调用APP的main函数执行。
PS：此处需要说明一点的是笔者所实现的APP功能仅有小灯闪烁功能。原因是笔者所用的板子为STM32F103C8T6，其flash大小为64K、RAM大小为20K；上述已经说明过笔者boot区的RAM已经分配16K，仅剩余4K的容量。因此只能实现小灯闪烁功能（并且还是经过裁剪的）；如果大家想要丰富APP功能的，建议换容量更大的ZET6板子，并且解压缩APPV1.1.rar压缩包--打开keil工程-，自行丰富其功能。

#### 7.3.5 带CRC32校验的固件升级代码详解

￼
通过前面章节的讲解，我们已经知道了普通固件升级的流程以及原理。而CRC校验实际上就是在执行拷贝和跳转APP操作之前，先校验一下接收的固件是否正确，具体的升级软件框架见第5.2章节。
（1）CRC32算法原理
我尽量用两段话说清楚，如果大家觉得还是不够明白，请自行百度搜索。循环冗余校验（CRC）是一种基于二进制多项式除法的差错检测编码技术。其核心思想是将待发送的数据比特序列视为一个二进制多项式，发送方和接收方事先约定一个生成多项式（即除数）。发送方在数据末尾附加若干个零（等于生成多项式最高次幂的个数），然后用生成多项式对该扩展后的数据进行模2除法（即异或运算），得到的余数即为CRC校验码。随后，发送方将原始数据与CRC码拼接，构成完整的发送帧。
接收方收到数据后，可以提取数据帧中的原始数据部分，然后使用相同的CRC32算法重新计算出一个校验值，再把这个新计算出的值与发送方附在帧尾的CRC32值进行比对。如果两者相同，则判定传输过程中没有错误；如果不同，则说明数据在传输中发生了改变。
简单来说，发送方需要把原本固件数据异或上某个给定的多项式计算出余数，这个余数就是CRC。然后把原本固件数据加上CRC码构成新的固件数据。而接收方接收到新的固件数据之后，把原始的固件数据分离出来，再通过CRC32算法规则计算出一个新的余数CRC（新）；最后，将这两个CRC相比较，比较通过就说明固件数据校验成功，否则失败。
（2）CRC32算法规则
接下来说一下接收端的CRC32算法如何计算出新余数。CRC32算法有查表法和直接计算法，由于查表法计算效率较高，因此本项目使用查表法进行实现，算法流程如下：
1、预计算表：事先计算一个256项的查找表，每一项对应一个索引值（0-255）CRC32中间结果，并将结果存入表。
2、初始化：CRC寄存器设为0xFFFFFFFF。
3、逐字节查表：遍历每个数据字节，将当前字节与CRC寄存器的低8位异或，得到一个索引值（0~255）；用该索引查表得到对应的32位值；然后将CRC寄存器右移8位，再与查表值异或，作为新的CRC值。
4、后处理：所有字节处理完后，将结果取反。
对应的算法实现函数如图7.39：

![image-20260611231814475](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231814475.png)

图7.39
（3）新旧CRC码比对操作
见图7.40源码：

![image-20260611231828722](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611231828722.png)

通过上图中的函数，即可对比新旧CRC码，如果相同，则校验成功。主要解释一下从数据末尾提取CRC32操作。由于CM3架构是小端模式，因此需要低字节存放在低地址，高字节存放在高地址；而data[firmareLen]代表开始地址，因此此处要存放低字节。举例：如一串数据0x1234567，0X12属于高字节，0X67属于低字节。因此我们需要把data[FirmwareLen+3]这个数据（对应0x67，属于低字节）左移24位移动到低地址处（data[firmwareLen]），其他的三位也是同理。
扩展：如果是大端模式，就是低字节要存放在高地址，高字节要存放在地址！
（4）发送端处理
上述几节已经说完了接收端的校验处理操作，接下来说一下发送端（PC端）的处理。由于笔者采用的是串口IAP的方式来发送固件数据到接收端，因此需要使用串口调试助手软件，但该软件不能给要发送的固件数据自动附加CRC码。因此，笔者自己写了一个Python脚本，来对原来的固件数据附加上CRC码，以生成带CRC校验信息的新固件APP_CRC.bin。以上就是Python脚本所要解决的问题，Python源码如图7.41所示：

```c++
import sys​
2import zlib​
60    print("=" * 50)​
61    print("固件CRC32预处理工具")​
62    print("=" * 50)​
63    ​
64
￼    
if len(sys.argv) < 2:​
65        print("\n使用方法:")​
66        print("  python add_crc32.py <输入文件.bin> [输出文件.bin]")​
67        print("\n示例:")​
68        print("  python add_crc32.py app.bin")​
69        print("  python add_crc32.py app.bin app_with_crc.bin")​
70        print("\n说明:")​
71        print("  - 输入文件：原始的APP固件bin文件")​
72        print("  - 输出文件：附加CRC32后的bin文件（用于发送到STM32）")​
73        print("  - 如不指定输出文件名，默认为 '原文件名_crc.bin'")​
74        return​
75    ​
76    input_file = sys.argv[1]​
77    output_file = sys.argv[2] if len(sys.argv) > 2 else None​
78    ​
79    print(f"\n输入文件: {input_file}")​
80    add_crc_to_firmware(input_file, output_file)​
81​
82
￼
if __name__ == "__main__":​
83    main()
```

如果你有一定的Python基础，上图中的源码还是很容易看懂的。首先来看main函数（59行-80行源码），Python中用关键字def来定义函数。print类似于C语言中的printf，也是打印输出的意思；接下来是一个if判断语句，意思是如果用户输入的参数不足两个，就会打印出 “使用方法”。此处解释一下使用该Python脚本的使用方法：
0、前提是已安装好Python3.10以上版本的环境；
1、解压range_hod_project.rar到本地的某个文件夹下（此处以XXX为例）；
2、用vscode代码编辑器打开XXX文件夹；
3、打开vscode，点击扩展--在搜索框输入Python--选择第二个Python图标进行安装即可，如图7.42所示：

![image-20260611232214715](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232214715.png)

4、打开vscode终端，输入以下命令：

```c++
cd tools                        /*表示进入tools文件夹下*/​
2python add_crc32.py APP.bin    /*表示执行python脚本，APP.bin是输入的固件数据*/
```

输入命令以后，就会在tools文件夹下生成一个附带CRC码的新固件Bin文件（APP_crc.bin），仅需要该文件通过串口调试助手传输到STM32的串口即可（上述操作也可以看笔者的演示视频）；如果你实在不会上述操作也没关系，因为在tools文件夹下，笔者已经生成好了APP_crc.bin，你可以直接使用。
解释完使用方法后，继续回到图7.41的76-80行源码。首先定义了两个变量：
input_file：直接获取第一个参数作为输入文件路径；
output_file：如果用户提供了第二个参数则使用它，否则设为 None（后续函数会自动生成默认文件名）；
￼
最后调用以下函数add_crc_to_firmware生成新固件，该函数主要做了四件事情：读取原始bin文件、计算CRC32校验值、将CRC32附加到文件末尾、最后保存为新文件。

### 7.4 剩余的关键驱动层源码详解

￼
本章节笔者将讲解剩余的一些较难驱动层源码，包括直流有刷电机驱动与编码器测速（motor.c）与PID算法（pid.c）。未提到的其他驱动文件都比较简单，请自行查看分析。

#### 7.4.1 motor.c文件详解

￼
在阅读本小节之前，希望你能再加深一下STM32的定时器知识储备，主要是高级定时器的互补输出、死区刹车、编码器功能等，请查阅学习资料文件夹--DMF407电机控制专题教程_V1.0.pdf.
(1)直流有刷电机驱动
￼
一般而言，电机控制都是通过定时器输出PWM来进行控制的。而在STM32中，有高级定时器和通用定时器之分，高级定时器相较于普通定时器，多了互补输出和带死区刹车控制等功能，因此电机控制通常用高级定时器来进行控制。接下来先对高级定时器的互补输出、死区、刹车输入等基础知识进行介绍。
(1.1)互补输出
￼
在之前学习的STM32基础定时器知识中，可以通过定时器的某个通道CHX输出PWM波形。而在高级定时器中，每个CHX通道（x取1，2，3，4），存在着对应的CHXN通道，该通道称为互补输出通道，如图7.43所示：

![image-20260611232311867](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232311867.png)

图中，红色的波形代表CHX通道，绿色的波形代表CHXN通道。当CHX通道输出高电平时，CHXN输出低电平，这就称为互补输出。
(1.2)死区
￼        
带死区控制的互补输出指的是在CHX和CHXN输出的高低电平转换的等待时间，这段时间称为死区时间，只有死区时间过后，才能实现互补输出，如图7.44：

![image-20260611232324540](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232324540.png)

那为什么需要这段死区时间呢？答案还需要借助电机H桥来进行说明，带死区控制的互补输出通常用于电机控制的H桥中，H桥的简化示意图如7.45所示：

![image-20260611232340331](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232340331.png)

如上图H桥的原理表述为：Q1-Q4都是NPN型三极管。当采用互补输出时，OC1输出高电平期间，OCIN输出低电平。那么OC1输出高电平让Q1和Q4导通（原理看模电知识，此处不过多解释），那么此时电流经VCC流经Q1-->电机-->Q4-->GND，此时电机转动（假设正转）；而OC1N输出高电平时，此时OC1输出低电平，此时Q2、Q3导通，Q1和Q4截止，电流方向相反，电机反转。
但是当没有死区时间时，在OC1和OC1N的高电平--低电平（或者是低电平--高电平）转换期间，三极管由于关断不及时（关断是需要时间的），这就会造成H桥的同一侧三极管同时导通，此时会让VCC直接与GND连接，造成短路！因此，需要在两个通道进行高低电平转换的时候插入死区时间，等待三极管到达稳定状态，再进行互补输出。 
那又出现了新的问题，如何去计算这段死区时间呢？答案就在STM32高级定时器的寄存器中，一般使用CR1的CKD位和BDTR寄存器的UTG位来进行死区时间设置的，如图7.46所示：

![image-20260611232354935](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232354935.png)

举个例子：当UTG位的值为250时，250 的二进制数为11111010，即UTG[7:5]为111，所以选择第四条公式：DT=(32+ DTG[4:0]) * Tdtg，其中Tdtg = 16 * TDTS。而TDTS的值为CKD位的选择，如CKD=10时，此时为4分频。
(1.3)刹车输入
￼        
刹车的概念即是字面意思，当配置一个IO口为刹车输入时，如果刹车有效，此时CHX和CHXN通道就不会输出PWM信号去驱动电机转动，此时电机就会慢慢停止。这就称为刹车，它也是由BDTR寄存器来进行设置的，如下图7.47：

![image-20260611232409342](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232409342.png)

 可见，当刹车输入有效时，此时MOE位会被硬件清零，此时会禁止OC和OCN输出或强制为空闲状态。
￼
(1.4)直流有刷电机
￼
在了解高级定时器的互补输出使用基础知识之后，接下来看一下如何通过互补输出去驱动直流有刷电机转动。直流有刷电机BDC是一种内含电刷装置，可以将直流电能转换成机械能的电动机。 其结构由定子、转子、电刷和换向器组成。其中，定子用于产生固定的磁场，通常由永磁体或电磁绕组制成；转子由一个或多个铜线绕组构成，通电后可以在磁场中受力运动；电刷指的是将外部电流输入到转子绕组上；换向器用于改变转子绕组中电流的流向。了解结构之后来看一下直流有刷电机的工作原理。
原理如下：本质上是通电导线在磁场中受力运动，因此需要通过左手定则来进行判断（是否感觉到死去的高中物理知识在攻击你）。左手定则是判断通电导线处于磁场中时，所受安培力 F (或运动)的方向、磁感应强度B的方向以及通电导体棒的电流I三者方向之间的关系的定律。 左手定则的具体内容：将左手四指并拢伸直，使拇指与其他四指在平面内垂直，手掌方向 代表磁场的方向（从N级到S级），四指代表电流的方向（从正极到负极），那拇指所指的方向就是受力的方向，如图7.48所示：

![image-20260611232424048](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232424048.png)

知道左手定则后，来看直流有刷电机的简化示意图，如下所示：

￼
￼![image-20260611232437041](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232437041.png)

由图可见，当电流从C端流入，D端流出时；此时根据左手定则可知a1a2线圈往上运动，b2b1线圈往下运动；当整个线圈转动90°后，此时换向器C和D的位置交换，那么此时电流从D端流入，C端流出，导致线圈继续沿着原来的方向受力转动，这就是直流有刷电机基本工作原理。        
那么从上可知，电机想要调节速度时，即调节线圈的受力大小，也就是调节电流大小（即调节电源电压大小）；电机想要换向时即调节电流方向。了解直流有刷电机的基本工作原理之后，接下来使用H桥电路来进行电机驱动。
(1.5)直流有刷电机方向及速度控制原理
￼  
H桥电路可方便对电机的正反转方向及速度进行控制，具体原理如下：电机方向控制原理不再过多叙述（见1.2章节）；而电机调速原理是固定一路输入信号的电平，另一路信号利用PWM波作为输入，这样即可调节驱动板输出到电机的电压，从而实现电机的速度控制。波形如图7.49所示：

![image-20260611232448698](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232448698.png)

具体来说，比如CH1固定为高电平时，CH1N输出PWM波，当CH1N为低电平周期时，此时电机会转动；这时CH1N转换为高电平周期时，由于CH1和CH1N都输出高电平，那么此时电机就会减速，但由于惯性电机不会立即停止。因此，可通过调节CH1N的PWM占空比来实现速度控制，即占空比越大，高电平周期越长，电机转动的就慢。
综上，可通过TIM1_CH1（主通道）输出PWM波，TIM1_CH1N（互补通道）固定输出高电平（后续讲如何实现），此时只要调节主通道输出的 PWM 占空比即可调整电机上的电压，进而控制电机的速度。当电机需要换向的时候，我们就让主通道固定输出高电平，互补通道输出PWM即可。
(1.6)直流有刷电机驱动的实现
￼
了解上述直流有刷电机的调速、换向原理之后，接下来先介绍电机调速时如何让某通道固定输出高/低电平。根据直流有刷电机调速原理，需要将一路输出固定电平，另外一路输出PWM波。为了方便同时对方向和速度进行控制，采用高级定时器的互补输出方式。但互补输出是主通道输出PWM方波时，互补通道输出相反的PWM方波。那如何做到在主通道输出PWM方波时，互补通道固定输出一个高电平或者低电平呢？答案还是藏在BDTR的寄存器的OSSR位和OSSI位里，如图7.50：

![image-20260611232458733](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232458733.png)

上图中OSSR位代表的是当OSSR=1，定时器在运行模式下（计数使能的状态）如果突然关闭了互补输出中的某个通道（CCXE或CCXNE=0），那么此时被关闭通道的CH1或者CH1N将输出无效电平（通过PWM驱动代码中极性的设置来决定，如果极性设置的是高，代表1为有效电平，0为无效电平）；同理，OSSI位代表的是定时器在空闲模式下（定时器不工作），如果设置为1，关闭对应通道时，对应通道输出空闲电平（具体看CR2寄存器，此处不过多展开）。更加具体来说，手册中表75对OSSR和OSSI的设置状态做了总结，如图7.51：

![image-20260611232510643](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232510643.png)

从上表可以看出，当MOE位为1且OSSR为1时，如果PWM的主输出通过和互补输出通道有一方未使能，则输出无效电平；如果两方都未使能，则输出禁止，此时没有输出，电机不会转动。通过上边6个章节的讲解，再来看电机的驱动、停止、换向以及速度调节函数代码就很好理解了。
(2)编码器测速驱动
￼
在电机控制中，常需要对电机的转速进行检测，一般采用编码器测速的方式。
编码器是一种能将直线位移、角位移数据转换为脉冲信号、二进制编码的设备。 它本质上就是一个传感器，可以把角位移或直线位移转换成电信号，并反馈给控制器，使控制 器知道当前机械运动的位置、角度等信息。        
编码器常用的有磁电增量式、光电增量式以及光电绝对式三种，笔者主要讲解磁电增量式编码器，其原理是：利用霍尔效应，将位移转换成计数脉冲，用脉冲个数计算位移和速度。如图7.52所示：

![image-20260611232522949](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260611232522949.png)由图可见，磁电增量式编码器的结构包含：磁盘、霍尔传感器以及信号转换电路3个部分，其中，磁盘是由一个个交替排布的S极和N极磁极组成；霍尔传感器可以把磁场的变化转换成电信号的变化，它通常有A、B两相（有的还有Z相），这两相的安装位置形成一定的夹角，这使得输出的A、B两相信号有90°的相位差；信号转换电路可以把电信号转换成脉冲信号。        
一般而言，磁盘会装在电机的转轴上，它会随着电机的转轴旋转，而磁盘上面的S极和N极就会交替地经过霍尔传感器的A、B两相，霍尔传感器就可以把磁盘上的磁场变化转换为电信号的变化（霍尔传感器原理不过多展开，有兴趣自行百度霍尔效应），输入到信号转换电路中，经过信号的转换之后，我们就可以得到A、B两相脉冲信号。从上图中可以看到，A、B两相脉冲信号存在90°的相位差，而磁盘的正反转方向就决定了是A相信号在前还是B相在前。 了解编码器的基本结构后，来看一下编码器的基本参数。如下所示：        
分辨率：编码器每个计数单位之间产生的距离，它是编码器可以测量到的最小的距离。对于增量式编码器，分辨率表示为编码器的转轴每旋转一圈所输出的脉冲数（PPR）， 也称为多少线，本篇文章以11线为例。         
精度 ：编码器分辨率和精度是两个独立的概念，精度是指编码器输出的信号数据与 实际位置之间的误差，常用角分′、角秒″表示。       
 最大响应频率：编码器每秒能输出的最大脉冲数，单位Hz，也称为PPS。        
最大转速：指编码器机械系统所能承受的最高转速。
(2.1)编码器的测速原理
￼
在第2章节讲过，直流有刷电机的编码器有A、B两相。它们会输出两个相位差为90°的脉冲。一般来讲，当电机正转时，A相脉冲在前；当电机反转时，则是B相脉冲在前。那此时问题来了，经过编码器中的霍尔传感器检测到磁场，转变为脉冲信号后，如何将这些脉冲信号转换为电机的转速呢？        
如章节2所示，电机转轴上布满了磁铁，当电机转一圈时，安装在电机转轴上的霍尔传感器可以检测到11个脉冲；而电机转速是1min转多少圈，因此只需要去计算1min内检测到的脉冲数，再除以分辨率，就可以得到1min转动的圈数，即电机的转速。        
因此，问题的关键又转移到了如何去计算出1min内的脉冲数。好在STM32的定时器里有个编码器接口模式功能可以解决这个问题，STM32定时器功能如图7.53所示：

￼
￼![image-20260612091237465](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091237465.png)

依据学习资料文件夹--STM32中文参考手册_V10，编码器接口模式基本上相当于使用了一个带有方向选择的外部时钟。这意味着计数器只在0到 TIMx_ARR寄存器的自动装载值之间连续计数(根据方向，或是0到ARR计数，或是ARR到0计数)。个人解释来说，编码器接口模式就是一个计数器，对外部输入的脉冲信号进行计数；而计数的方向则是由脉冲相位的先后所决定的，计数范围在0-65535之间。如图7.54所示：

![image-20260612091300694](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091300694.png)

由图可见，当电机正转时，输出两相脉冲信号，A相脉冲在前，此时编码器接口把脉冲信号作为计数器的脉冲，计数方式为递增计数；当电机反转时，计数方式就变成了递减计数。 该计数方向的规定也可以在STM32中文参考手册_V10的表77找到，如图7.55所示：

￼![image-20260612091313938](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091313938.png)
￼接下来以一个示例来对上表进行说明，当A相上升沿到来时（如图7.56中①处），我们需要关注B相的电平高低，从下图中可看到B相此时是低电平，结合表77，可以得知此时计数方向为递增计数；当A相下降沿到来时（图7.56中②处），从图中可以看到B相此时是高电平，结合表77，可以得知此时计数方向为递增计数；当A相上升沿再次到来时（图7.57中③处），同理可得此时计数方向为递增计数。 综上所得，可以知道此时电机正转对应的计数方向就是递增计数。

![image-20260612091332261](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091332261.png)

此外，还需要注意一点的是关于计数模式的选择：如果选择仅在A相或者B相处计数，就相当于对脉冲信号进行了2倍频（因为此时会在两个边沿计数，上升沿和下降沿）， 此时如果编码器输出10个脉冲信号，那么就会计数20次。如果选择的是在A和B处均计数，就相当于对脉冲信号进行了4倍频，此时如果编码器输出10个脉冲信号，那么就会计数 40 次。因此，通过计数次数来计算电机速度的时候，需要除以相应的倍频系数。        
综上可知如何计算1min内的脉冲数后，即可得到电机转速的具体公式如下：电机转速 = 一分钟内计数变化量 / 倍频系数 / 编码器线数 / 减速比；其中，减速比指的是瞬时输入速度与输出速度的比值，如输入转速为1500r/min，输出转速为25r/min，那么其减速比则为60:1。
(2.2)编码器接口
￼
了解到编码器接口模式的计数原理之后，又出现了个问题--即外部输入的脉冲信号是怎么进入到编码器接口的呢？也可以在参考手册找到答案，定时器的编码器接口框图如图7.58所示：

![image-20260612091359760](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091359760.png)

由上图可见，A、B两相脉冲信号从TIMx_CH1和TIMx_CH2这两个通道输入，经过滤波器 和边沿检测器（可以设置滤波和反相）的处理，进入到编码器接口控制器中。此外， TIMx_CH3和TIMx_CH4是不支持编码器接口模式的。综上，了解了如何计算脉冲数变化量从而得到电机转速，接下来看一下具体的代码实现需要哪些寄存器的参与。
(2.3)关键寄存器
￼
先来看TIMx_CCMR1寄存器，如图7.59所示：

![image-20260612091415621](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091415621.png)

需要操作的关键位由上图可见，主要解释一下滤波器的设置。假设IC1F[3:0]=0011，并设置IC1映射到TI1上。表示以fCK_INT为采样频率，当连续8次都是采样到TI1为高电平或者低电平，滤波器才输出一个有效输出边沿。当8次采样中有高有低， 那就保持原来的输出，这样可以滤除高频干扰信号，从而达到滤波的效果。接下来看捕获/比较使能寄存器TIMx_CCER，如图7.60所示：

![image-20260612091429604](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091429604.png)

由上图可见，需要注意的是CC1位，当配置为输入时，可以选择反相或者不反相，选择反相时代表电机转动方向与编码器计数方向不一致时，可以通过设置为反向来快速纠正方向。 最后关注从模式控制寄存器TIMx_SMCR，如图7.61所示：

![image-20260612091441630](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091441630.png)

该寄存器的SMS[2:0]位，用于从模式选择，其实就是选择计数模式。如果需要仅在 TI1边沿处计数（A接TI1），则设置SMS[2:0]=010；如果需要仅在 TI2边沿处（B接TI2）计数，则设置SMS[2:0]=001；如果需要在 TI1、TI2边沿处都计数，则设置SMS[2:0]=011，即4倍频，笔者项目中使用的就是4倍频。
(2.4)驱动代码编写
￼
1、编码器初始化函数：

```c++
/* TIM2编码器功能初始化 */​
2void TIM2_encode_init(u16 arr, u16 psc)​
14    TIM_TimeBaseStructure.TIM_Period = arr;                 //占空比​
15    TIM_TimeBaseStructure.TIM_Prescaler = psc;              //分频系数​
16    TIM_TimeBaseStructure.TIM_ClockDivision = TIM_CKD_DIV1; //时钟不分割​
17    TIM_TimeBaseInit(TIM2, &TIM_TimeBaseStructure);​
18    ​
19    /* 配置编码器，此处选择4倍频，需要TI1(A相)和TI2(B相)进行计数 */​
20    TIM_ICInitStructure.TIM_Channel = TIM_Channel_1;                // 选择CH1          ​
21    TIM_ICInitStructure.TIM_ICFilter = 10;                          // 输入滤波器​
22    TIM_ICInitStructure.TIM_ICPolarity = TIM_ICPolarity_Rising;     // 选择是否反相，不反相​
23    TIM_ICInitStructure.TIM_ICSelection = TIM_ICSelection_DirectTI; // 选择输入通道，对应寄存器CCIS[1:0]​
24    TIM_ICInitStructure.TIM_ICPrescaler = TIM_ICPSC_DIV1;           // 不分频，每一个边沿触发一次捕获​
25    TIM_ICInit(TIM2,&TIM_ICInitStructure);                          // 初始化TIM2输入捕获接口​
26    ​
27    TIM_ICInitStructure.TIM_Channel = TIM_Channel_2;                // 选择CH2          ​
28    TIM_ICInitStructure.TIM_ICFilter = 10;                          // 输入滤波器​
29    TIM_ICInitStructure.TIM_ICPolarity = TIM_ICPolarity_Rising;     // 选择是否反相，不反相​
30    TIM_ICInitStructure.TIM_ICSelection = TIM_ICSelection_DirectTI; // 选择输入通道，对应寄存器CCIS[1:0]​
31    TIM_ICInitStructure.TIM_ICPrescaler = TIM_ICPSC_DIV1;           // 不分频，每一个边沿触发一次捕获​
32    TIM_ICInit(TIM2,&TIM_ICInitStructure);                          // 初始化TIM2输入捕获接口​
33    ​
34    /* 配置编码器接口，选择TI1和TI2同时计数 */​
35    TIM_EncoderInterfaceConfig(TIM2, TIM_EncoderMode_TI12, TIM_ICPolarity_Rising, TIM_ICPolarity_Rising);​
36    ​
37    /* 配置TIM2中断，因为计数器为65536，可能会溢出所以配置中断 */​
38    NVIC_InitStructure.NVIC_IRQChannel = TIM2_IRQn;                 // TIM2中断​
39    NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 6;       // 抢占优先级​
40    NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;              // 子优先级0级​
41    NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;                 // IRQ通道被使能​
42    NVIC_Init(&NVIC_InitStructure);                                 // 根据NVIC_InitStruct中指定的参数初始化外设NVIC寄存器​
43    ​
44    TIM_ITConfig(TIM2,TIM_IT_Update,ENABLE );                       // 使能指定的TIM2中断，允许更新中断​
45​
46    TIM_Cmd(TIM2, ENABLE);                                          // 使能TIM2​
47    ​
48}
```

3、同时用于计算转速的TIM4定时器中断初始化函数如下，时间为1 ms：

```c++
代码块​
C
￼
1/* 定时器Timer4初始化，用于计算转速 */​
2void TIM4_init(u16 arr,u16 psc)​
3
￼
{​
4    TIM_TimeBaseInitTypeDef  TIM_TimeBaseStructure;​
5    NVIC_InitTypeDef NVIC_InitStructure;​
6​
7    RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM4, ENABLE);            // 时钟使能​
8​
9    TIM_TimeBaseStructure.TIM_Period = arr;                         // 设置在下一个更新事件装入自动重装载寄存器周期的值  ​
10    TIM_TimeBaseStructure.TIM_Prescaler =psc;                       // 设置用来作为TIMx时钟频率除数的预分频值  ​
11    TIM_TimeBaseStructure.TIM_ClockDivision = 0;                    // 设置时钟分割:TDTS = Tck_tim​
12    TIM_TimeBaseStructure.TIM_CounterMode = TIM_CounterMode_Up;     // TIM向上计数模式​
13    TIM_TimeBaseInit(TIM4, &TIM_TimeBaseStructure);                 // 根据TIM_TimeBaseInitStruct中指定的参数初始化TIMx的时间基数单位​
14    TIM_ITConfig(TIM4,TIM_IT_Update,ENABLE );                       // 使能指定的TIM4中断，允许更新中断​
15    ​
16    NVIC_InitStructure.NVIC_IRQChannel = TIM4_IRQn;                 // TIM4中断​
17    NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 5;       // 抢占优先级​
18    NVIC_InitStructure.NVIC_IRQChannelSubPriority = 0;              // 子优先级0级​
19    NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;                 // IRQ通道被使能​
20    NVIC_Init(&NVIC_InitStructure);                                 // 根据NVIC_InitStruct中指定的参数初始化外设NVIC寄存器​
21​
22    TIM_Cmd(TIM4, ENABLE);                                          //使能定时器                         ​
23}
```

4、初始化好1 ms中断以后，当触发TIM4中断函数时，会触发一次信号量同步，跳转到速度计算任务执行逻辑（原理分析见前边章节）。在速度计算任务中，会先调用获取编码器计数值函数，如下代码所示

```c++
￼
1/* 获取编码器计数值 */​
2int get_encoder_value(void)​
3
{​
4    u32 buffer;​
5    buffer=TIM_GetCounter(TIM2)+(overflow*65536);​
6    return buffer;​
7}
```

从上可以看出，编码器的计数值为当前TIM2定时器的当前计数值再加上溢出的次数。
5、获取编码器计数值后，接下来调用速度计算函数，代码如下：

```c++
￼
1/* 获取电机转速。ms=50表示每50ms计算一次速度，即采样精度为50ms*/​
2float get_speed(int encode_value,u16 ms)​
3
{​
4    u8 i = 0, j = 0;​
5    float temp = 0.0;​
6    static float speed=0;                                   /*需要为静态变量，因为一阶滤波算法需要上次的滤波值 */​
7    static uint8_t sp_count = 0, k = 0;​
8    static float speed_arr[10] = {0.0};                     // 存储速度进行滤波数组 ​
9    static int old_value=0,now_value=0;​
10​
11    if (sp_count == ms)                                     // 计算一次速度 ​
12
    {​
13        now_value = encode_value;                           // 记录当前编码器值​
14        ​
15        // 计算转速，30为减速比，4倍频，11线​
16        ​
17        /* 1000/ms指在这个ms时间内获得了xx脉冲变化值，用xx变化值/ms得到1ms的脉冲变化值​
18         * 再乘以1000得到1s的变化值 */​
19        speed_arr[k++] = (float)((now_value - old_value) * ((1000 / ms) * 60.0) / 30 / (11*4)); ​
20        old_value = now_value;                              // 保存当前计数值​
21        ​
22        /* 累计10次速度值，利用冒泡排序，后面做中值滤波 */​
23        if (k == 10)​
24
        {​
25            for (i = 10; i >= 1; i--)                       ​
26
            {​
27                for (j = 0; j < (i - 1); j++) ​
28
                {​
29                    if (speed_arr[j] > speed_arr[j + 1])    /* 数值比较 */​
30
                    { ​
31                        temp = speed_arr[j];                /* 数值换位 */​
32                        speed_arr[j] = speed_arr[j + 1];​
33                        speed_arr[j + 1] = temp;​
34                    }​
58    return speed;​
59}
```

以上所示的代码也很简单，首先就是将当前的编码器计数量与之前的编码器计数量相减得到脉冲计数变化量，然后按照2.1节所讲的电机转速计算公式计算出电机转速，并保存在数组里；之后进行冒泡排序以及一阶滤波，以获得准确的速度值。

#### 7.4.2 pid.c文件详解

￼
先来介绍PID算法解决了什么问题？在之前讲解的直流有刷电机驱动中，如果电机的负载没有改变，那么只需要设置固定的占空比，电机速度就会稳定在目标范围。然而，在实际的应用中，负载可能会发生变化（比如说灰尘堆积、增加了重物在电机上等），此时如果还是输出固定的电压，电机的速度就偏离目标范围了，为了解决这个问题，引入了PID算法，其作用是当系统受到外界干扰而偏离正常状态时，自动调节回正常范围内。常用于水壶保温系统、大棚温控系统、水位控制系统、电机控制系统等。
(1)什么是PID
￼
PID就是Proportion（比例）、Integral（积分）、 Differential（微分），其控制流程图如下所示

![image-20260612091727378](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091727378.png)

先来说比例，其公式为u=kp*e。u为输出、kp为比例系数、e为偏差。根据公式可看出输出u与偏差e成正比。以电机转动为例来说明，假设我期望输出的转速是60，但实际的转速是40，现在偏差是20，那此时kp越大，输出就越大，就能越快达到目标转速。但kp不是越大越好，如果kp越大，此时会出现超调或者振荡，会使得系统的稳定性下降。        
然而仅有比例环节还不够，因为会存在静态误差。静态误差指的是假设现在需要调节棚内温度为30℃，而实际温度为25℃，此时偏差e=5，Kp为固定 值，如果此时的输出可以让大棚在半个小时之内升温5℃，而外部的温差可以让大棚在半个小 时之内降温 5℃，也就是说，输出 u 的作用刚好被外部影响抵消了，这就使得偏差会一直存在，这个误差就称为静态误差。        
因此，为了消除静态误差，引入了积分环节。积分环节可以对偏差e进行积分，只要存在偏差，积分环节就会不断起作用，主要用于消除静态误差，提高系统的无差度。引入积分环节后，比例+积分环节的公式如下：

![image-20260612091743108](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091743108.png)

加入积分环节后，假设比例环节的作用被抵消，一直存在静态误差；那么此时积分环节就会一直累加误差，从而增大输出u，消除静态误差。从公式可以看出，当偏差e累加值或者是积分系数越大，此时输出u就越大，消除静态误差的时间就越短。        
同理，积分系数过大，也会引起超调现象，因此积分系数不是越大越好。在积分环节中，只要存在偏差e，就会不断累加偏差值，直到偏差为0。此时累积偏差不再变化，但积分环节依然再发挥作用，因此易引起超调现象。为了解决上述问题，引入了微分环节提前去抑制输出。加入微分环节后公式如下所示：

![image-20260612091757551](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091757551.png)

从上述公式可以看出，微分环节是根据偏差变化量提前做出相应控制，以减小输出，抑制超调。如第n次的偏差减去n-1次的偏差=-3，代入上述公式可知，微分环节会削弱比例环节和积分环节的作用。
PS：上述所提供的PID公式为位置式公式，本项目中的用的PID算法也是位置式算法，PID有位置式和增量式之分，增量式PID请自行查阅学习资料文件夹--DMF407电机控制专题教程_V1.0.pdf.
(2)PID算法代码实现
￼
代码如下所示：

```c++
/**​
2 * @brief PID计算（位置式）​
3 * @note 位置式PID公式：output = Kp*e(k) + Ki*Σe(k) + Kd*[e(k)-e(k-1)]​
4 * @param pid: PID结构体指针​
5 * @param actual: 当前实际值​
6 * @return PID输出值​
7 */​
8float PID_Calculate(PID_TypeDef *pid, float actual)​
9
￼
{​
10    float p_out, i_out, d_out;​
11    ​
12    /* 更新实际值 */​
13    pid->actual = actual;​
14    ​
15    /* 计算当前误差 */​
16    pid->error = pid->target - pid->actual;​
17    ​
18    /* 积分累加 */​
19    pid->integral += pid->error;​
20    ​
21    /* 积分限幅，防止积分饱和 */​
22    if (pid->integral > pid->integral_max)​
23
￼
    {​
24        pid->integral = pid->integral_max;​
25    }​
26    else if (pid->integral < -pid->integral_max)​
27
￼
    {​
28        pid->integral = -pid->integral_max;​
29    }​
30    ​
31    /* 计算PID三个分量 */​
32    p_out = pid->Kp * pid->error;                           /* 比例项 */​
33    i_out = pid->Ki * pid->integral;                        /* 积分项 */​
34    d_out = pid->Kd * (pid->error - pid->last_error);       /* 微分项 */​
35    ​
36    /* 计算PID输出 */​
37    pid->output = p_out + i_out + d_out;​
38    ​
39    /* 输出限幅 */​
40    if (pid->output > pid->output_max)​
41
￼
    {​
53}​
54​
```

(3)PID参数整定
￼
PID算法功能中最难的地方就是调参，因为每一个系统的比例系数、积分系数以及微分系数并不是通用的。因此对于每一个系统，都需要调整合适的PID参数。
(3.1)PID各个系数调节效果 
比例系数：调节作用快，系统一出现偏差，调节器立即将偏差放大输出。
￼
积分系数：积分系数的调节会改变输入偏差对于系统输出的影响程度。积分系数越大， 消除静差的时间越短，但是过大的积分系数则会导致系统出现超调现象，这在具有惯性的系 统中尤为明显。     
 微分系数：微分系数的调节是偏差变化量对于系统输出的影响程度。微分系数越大， 系统对于偏差量的变化越敏感，越能提前响应，进而抑制超调，但是过大的微分系数则会让整个系统出现振荡。         
上述先对PID的各个系数的调节效果做一个基本了解，接下来讲解具体的实操步骤。
(3.2)调参步骤
￼
工具准备： 串口CH340、上位机、开发板以及编写的代码。此处的上位机个人用的是VOFA＋,当在上位机中连接好串口之后，可以将串口打印的数据进行波形的实时化显示。
因为需要将数据通过串口波形化显示，因此需要添加串口输出函数，如下代码所示：

```
码块​
C
￼
1                     /* PID速度环控制 */​
2                        if(g_pidEnable)​
3
￼
                        {​
4                                pid_output = PID_Calculate(&g_speedPID, speed);                // 计算PID输出​
5                                motor_pwm_set(pid_output);                                                        // 设置电机PWM​
6                        }​
7                        ​
8                        /* 显示实际转速 */                                                                ​
9                        printf("%.1f,%.1f,%.1f\n", ​
10                               (float)g_targetSpeed, ​
11                                               speed, ​
12                                           pid_output);
```

注意：上述串口打印函数不能有文字符号，以及\r符号，否则不能实时显示波形。此处打印3个值，分别是设定的目标电机转速、实际电机转速及PID输出（PWM占空比），因此波形显示有3个通道。
1、调比例
先将代码编译好烧录到开发板，连接好串口，打开VOFA+上位机。接下来先将3.2节中的目标电机转速设置成电机最大转速的60%-70%，也可以稍微设置大一点，比如额定转速是250转，可以将目标值设定为220。然后将代码中的积分系数Ki以及微分系数Kd、Kp置0，开始先调节比例系数Kp，表示为纯比例调节。        
步骤为先增大Kp系数，直到输出值趋近于目标值（一定要让输出值，也就是实际转速要接近于目标转速），且系统出现震荡，也就是PID输出曲线出现震荡，如下图3.2.1所示：

![image-20260612091929811](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091929811.png)

如上图所示，这是上位机显示的3个值的波形，蓝色的为PID输出，紫色的为目标值，绿色的实际值。可以明显的看到蓝色波形在剧烈震荡，接下来逐渐减小比例系数Kp，直到曲线的大幅度振荡现象消失，但是曲线依旧存在小幅度的超调现象，并且此时通过调节比例系数已经无法优化曲线（因为要让目标转速尽量趋近于实际转速）。如图3.2.2所示：

![image-20260612091943931](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091943931.png)

2、调积分
从图3.2.2可以看出，在纯比例调节下，震荡现象已经大幅减小，记录下此时的比例系数Kp。但同时我们看到实际值为109，目标值为150，说明存在很大的静态误差。因此，接下来需要将Kp变成刚刚记录下来的值，微分系数Kd依然为0，同时增大积分系数Ki，直到系统再次出现震荡现象，如图3.3.3所示：

![image-20260612091954525](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612091954525.png)

接下来，再逐渐减小积分系数Ki，直到震荡现象大幅度消失，且目标转速与实际转速近似相等，此时记录下积分系数Ki。如图3.3.4：

![image-20260612092018552](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612092018552.png)

3、调微分
可以看到上图中振荡现象已经大幅消失了，还存在一些微小的超调现象。那说明此时需要开始按照之前的步骤调节微分系数Kd了。如果这时得到的PID输出曲线已经较为理想了，说明该系统仅需要比例和积分环节的控制，不需要微分参与。如果还存在一些微小的超调现象，就需要引入微分环节，此时将微分系数从0开始慢慢增大，直到系统曲线较为理想，记录下此时的微分系数。最终呈现的效果如图3.3.5所示：

![image-20260612092034730](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612092034730.png)

4、总结
PID的调参方法还有很多种，本次仅使用的是一般的调节方法，还包括试凑法、临界比例法等，有兴趣的可以自行搜索。无论采用哪一种方法所得到的PID参数，都需要在实际运行中进行最后调整与完善，因此，上述方法只能作为参考，并不是通用的，因此在PID 调参过程中， 一定要注意经验的积累。

#### 7.4.3 风速融合算法详解

￼
传统烟机只有一个档位开关，用户得自己判断油烟大小手动调档，但当油烟浓度变化时，要么吸不干净，要么噪音过大导致体验差。为了解决该痛点，引入了风速融合算法，即自动感知环境来决定烟机转动多快。但问题来了：厨房环境怎么量化？温度、湿度、气体浓度，三路传感器数据，怎么融合成一个“风速值”？为什么要做传感器融合？
(1)为什么要做传感器融合
￼
先来回答该问题。笔者项目中使用了三个传感器：温湿度传感器加气体浓度传感器；如果用单一传感器决策，会出现以下问题：
•
温度高不一定有油烟（夏天开火也热）
•
湿度大不一定有油烟（煮汤）
•
气体浓度高才最接近“炒菜”场景，但响应有延迟
所以必须综合三路信息，才能准确判断“是否在炒菜”，以及“该转多快”。
(2)算法核心原理
￼
先来看风速算法函数，如下代码所示：

```
/**​
2 * @brief 更新传感器数据并计算风速​
3 * @param temp: 温度值 (℃)​
4 * @param humidity: 湿度值 (%)​
5 * @param gas: 气体浓度值 ​
6 */​
7void WindSpeed_Update(u8 temp, u8 humidity, float gas)​
8
￼
{​
9​
10    /* 1. 传感器归一化 */​
11    /* 温度影响系数: f_T = (T - T_base) / (T_max - T_base) */​
12    g_windSpeedData.f_T = (temp - TEMP_BASE) / (TEMP_MAX - TEMP_BASE);​
13    g_windSpeedData.f_T = Constrain(g_windSpeedData.f_T, 0.0f, 1.0f);​
14    ​
15    /* 湿度影响系数: f_H = (H - H_base) / (H_max - H_base) */​
16    g_windSpeedData.f_H = (humidity - HUMIDITY_BASE) / (HUMIDITY_MAX - HUMIDITY_BASE);​
17    g_windSpeedData.f_H = Constrain(g_windSpeedData.f_H, 0.0f, 1.0f);​
18    ​
19    /* 气体浓度影响系数: f_G = (G - G_base) / (G_max - G_base) */​
20    g_windSpeedData.f_G = (gas - GAS_BASE) / (GAS_MAX - GAS_BASE);​
21    g_windSpeedData.f_G = Constrain(g_windSpeedData.f_G, 0.0f, 1.0f);​
22    ​
23    /* 2. 权重融合: F = w_t * f_T + w_h * f_H + w_g * f_G */​
24    g_windSpeedData.fusionValue = WEIGHT_TEMP * g_windSpeedData.f_T +​
25                                  WEIGHT_HUMIDITY * g_windSpeedData.f_H +​
26                                  WEIGHT_GAS * g_windSpeedData.f_G;​
27    ​
28    /* 3. 映射到PWM: PWM = PWM_min + (PWM_max - PWM_min) * F */​
29    g_windSpeedData.pwmValue = PWM_MIN + (PWM_MAX - PWM_MIN) * g_windSpeedData.fusionValue;​
30    g_windSpeedData.pwmValue = Constrain(g_windSpeedData.pwmValue, PWM_MIN, PWM_MAX);​
31    ​
32    /* 4. 判断是否为Cooking Event */​
33    if ((temp > COOKING_TEMP_THRESHOLD) &&​
34        ((humidity > COOKING_HUMIDITY_THRESHOLD) && (gas > COOKING_GAS_THRESHOLD)))​
35
￼
    {​
36        g_windSpeedData.isCookingEvent = 1;​
37    }​
38    else​
39
￼
    {​
40        g_windSpeedData.isCookingEvent = 0;​
41    }​
42}​
43​
```

从上图可以看出，算法原理分成三个步骤：
第一步：归一化
三个传感器的原始值量纲不同（温度℃、湿度%、浓度ppm），无法直接相加。需要映射到同一个尺度（0~1），表示“对风速的影响系数”。
第二步：加权融合
不同传感器对“油烟”的贡献不同，分别赋予权重，加权求和得到一个综合系数F。
第三步：线性映射
将综合系数F映射到PWM占空比（1%~100%），作为电机转速指令。公式如下：

￼
￼
￼


￼
￼
￼

其中：
◦
fT、fH、fG 是归一化后的影响系数
◦
wT、wH、wG 是权重，和为1
￼
(3)归一化设计
￼
每个传感器有自己的基准值和饱和值。如下所示：
温度归一化：

￼
￼
￼

笔者为了让系统更快反应，设定：20℃ → 系数0，35℃ → 系数1；高于35℃不再增加（饱和），低于20℃为0。
湿度归一化：

￼
￼
￼

同理，H_base=40%,H_max=75%。
气体浓度归一化：

￼
￼
￼

(4)权重确定与调试
那权重是如何确定的呢？笔者用的原则是：谁的物理意义更接近“油烟”，谁权重大。
◦
气体浓度：直接反映燃烧/油烟产物，权重 0.6
◦
湿度：炒菜产生水蒸气，有一定关联，权重 0.2
◦
温度：相关性较弱，权重 0.2
最终综合系数：

￼
￼
￼

关键调试点：一开始笔者把权重设成（0.33/0.33/0.33），电机不转动。后来改成（0.2/0.2/0.6），电机转动很灵敏，符合预期。
￼
(5)线性映射到PWM
￼
公式如下：

￼
￼
￼

PWMmin=20%，PWMmax=100%。此处有粉丝或许会问，为什么PWMmin不设置为0，或者是1%呢？
答案是：直流电机有启动电压，占空比太低电机不转。我实测最低10%能启动，但为了安全最终取20%作为最小运行转速（自动模式启动阶段）。

### 7.5 更新篇--bootloader固件升级

￼

#### 7.5.1 前言

￼
本次更新是因为有位粉丝朋友提出当进行固件升级之后，即跳转到APP区新固件之后；如果再次重新上电复位，此时程序又跳回boot区执行旧固件，并没有稳定在APP区执行，粉丝朋友认为这个流程是不完整的。期望结果是再次重新上电后，依然执行APP区的新固件，不想回到原来的boot区旧固件执行。因此，笔者根据这个需求进行更新，代码已同步更新到网盘中(rang_hood_projectv1.2)，请点击链接重新查看，详情看以下介绍。

#### 7.5.2 思想原理

￼
原来流程是：板子上电--执行到boot区main函数--执行旧固件--如果触发升级请求--进行升级跳转到APP新固件--如果再次重新上电--再次执行boot区main函数--继续回到boot区执行固件；
现更新流程：板子上电--执行到boot区main函数--执行旧固件--如果触发升级请求--进行升级跳转到APP新固件--如果再次重新上电--再次执行boot区main函数--完成硬件初始化后判断APP区是否存在固件、并且固件需要CRC校验通过--如果判断成功，跳转到APP区固件执行--否则回到boot区旧固件。在此流程下，即使在升级过程中发生断电，设备也不会变砖。因为再次重新上电时会先进入boot区的main函数，然后判断固件存在/校验通过，此时由于突然断电，校验就不会通过，就会回退到boot区的旧固件继续执行。
可能有粉丝朋友会问如何判断APP区存在固件并进行CRC校验的？难道是在第一次升级成功后用一个全局变量作为标志位吗？这肯定不行，因为全局变量的生命周期在进程结束后会销毁。因此答案是在Flash中新开一个标志存储区，里边保存第一次升级成功后的APP的固件长度、CRC值以及标志位(0xA5A5A5A5)。
在下一次重新上电时，先读这段存储区的Flash，查看标志位是否是0XA5A5A5A5(这个值并没有特殊含义，因为擦除后全为 0xFFFFFFFF，编程后全为0，所以用0xA5 二进制为 10100101，交替位模式来进行分辨)，如果不是则代表上一次升级未成功，可能写到一半flash掉电了。同时，会对比保存的CRC32值与重新计算的APP区的CRC值、对比固件长度，只有都对比通过才说明APP区的新固件没有问题，才可以选择跳转，否则回退到boot区旧固件执行，新的Flash分区布局如下图所示：

![image-20260612092238664](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612092238664.png)

#### 7.5.3 代码实现讲解

￼
整体上电流程如下图所示：

![image-20260612092257371](C:\Users\11624\AppData\Roaming\Typora\typora-user-images\image-20260612092257371.png)

先来看boot区main函数代码，在完成硬件初始化后，就会执行IAP_CheckAndJumpApp()函数去判断APP固件是否符合标准，只有符合标准了，才能继续跳转到APP区执行；否则直接返回，继续执行FreeRTOS旧固件代码。这里再多说一点，为什么要等硬件初始化完成之后，再去检查APP固件呢？原因是我在检查APP固件函数中添加了printf重定向调试，因此需要等串口初始化完成之后才能使用。如下图所示：

```
/*-----------------------------------------------------------​
2 * 主函数​
3 *----------------------------------------------------------*/​
4int main(void)​
5
￼
{ ​
6    /* 硬件初始化 */​
7    Hardware_Init();​
8    ​
9    /* 上电首先检查APP区固件是否有效，有效则直接跳转APP */​
10    IAP_CheckAndJumpApp();​
11    ​
12    /* APP无效或不存在，继续执行boot区 */​
13    /* 系统状态初始化 */​
14    System_Init();​
15    ​
16    /* 创建开始任务 */​
17    StartTask_Create();​
18    ​
19    /* 启动FreeRTOS调度器 */​
20    vTaskStartScheduler();​
21    ​
22    /* 正常情况下不会执行到这里 */​
23    while(1)​
24    {​
25    }​
26}
```

再来看一下IAP_CheckAndJumpApp()的源码，如下图：

```
void IAP_CheckAndJumpApp(void)​
2
￼
{​
3    u16 metaBuf[6];​
4    u32 magic, firmwareLen, storedCRC;​
5    u32 calculatedCRC;​
6  ​
7
￼
/*该宏用于擦除APP区和标志存储区Flash扇区，​
8*当第一次固件升级成功之后，之后的每次重新上电，​
9*仅会执行APP区代码，如果想要强制回退到boot区旧代码，需要开启​
10*此宏，赋值为1，然后重新编译烧录，就会执行擦除APP区和标志存储区，自动回退到boot区执行​
11*/    ​
12#if ERASE​
13    u32 addr;​
14    /*解锁Flash*/​
15     FLASH_Unlock();​
16    /*擦除APP区Flash*/​
17    for( addr = FLASH_APP1_ADDR; addr < FLASH_APP1_ADDR + FLASH_APP_MAX_SIZE; addr += 1024)​
18
￼
    {​
19        FLASH_ErasePage(addr);/*每次仅擦除一页，此处为1 KB*/​
20    }​
21     FLASH_ErasePage(FLASH_META_ADDR);​
22    /*上锁*/​
23    FLASH_Lock();​
24#endif​
25    ​
26
￼    
/* 从Flash读取元数据（6个u16），也就是读取标志存储区的flash​
27    *为什么是6个u16,因为F1的flash仅支持半字写入(2字节)，而标志存储区​
28    *有3个32位的变量，分别是固件长度、CRC值、标志位，换算成2个字节，就是6个半字​
29    */​
30    STMFLASH_Read(FLASH_META_ADDR, metaBuf, 6);​
31    ​
32    /* 显式按小端序重组为u32 */​
33    magic       = (u32)metaBuf[0] | ((u32)metaBuf[1] << 16);​
34    firmwareLen = (u32)metaBuf[2] | ((u32)metaBuf[3] << 16);​
35    storedCRC   = (u32)metaBuf[4] | ((u32)metaBuf[5] << 16);​
36    ​
37    /* 检查标志位是否有效 */​
38    if(magic != FIRMWARE_MAGIC)​
39
￼
    {​
40#if DEBUG​
41        printf("IAP_CheckAndJumpApp: No valid metadata\r\n");​
42#endif​
81​
82}  
```

接下来继续看IAP任务中新更新的代码，其实逻辑很简单，就是在第一次固件升级成功后，需要将升级成功标志，APP区的固件长度、APP的CRC32检验值写入到标志存储区的flash，这三个在代码中我统一称为元数据。代码如下所示：

```
/*省略部分代码*/​
2 for( addr = FLASH_APP1_ADDR; addr < FLASH_APP1_ADDR + FLASH_APP_MAX_SIZE; addr += 1024)​
3
￼
  {​
4      FLASH_ErasePage(addr);/*每次仅擦除一页，此处为1 KB*/​
5  }​
6​
7 iap_write_appbin(FLASH_APP1_ADDR,receive_buff,firmwareLen);//更新FLASH代码（只写入固件数据，不含CRC）​
8​
9 /*写入固件元数据到标志存储区，用于下次上电校验*/​
10 IAP_WriteMeta(firmwareLen, CRC32_Calculate(receive_buff, firmwareLen));​
11​
12 #if DEBUG​
13       printf("firmware updated complete, CRC32: 0x%08X!\r\n", CRC32_Calculate(receive_buff, firmwareLen));​
14 #endif
```

再继续深挖，来看IAP_WriteMeta的源码，如下所示：

```
/*--------------------------------------------------------------​
2*@brief: 写入固件元数据到Flash（0x0800EC00）​
3*@param firmwareLen: 固件长度（不含CRC）​
4*@param crc32Value: CRC32校验值​
5*@return: none​
6*@note: 使用STMFLASH_Write内部会处理页擦除​
7*       写入后会回读验证，确保数据正确​
8---------------------------------------------------------------*/​
9void IAP_WriteMeta(u32 firmwareLen, u32 crc32Value)​
10
￼
{​
11    u16 metaBuf[6];  /* 3个u32 = 6个u16 */​
12    u16 verifyBuf[6];​
13    u8 i;​
14    ​
15    /* 组装元数据：显式按小端序拆分为u16数组 */​
16    metaBuf[0] = (u16)(FIRMWARE_MAGIC & 0xFFFF);         /* magic低16位 */​
17    metaBuf[1] = (u16)((FIRMWARE_MAGIC >> 16) & 0xFFFF); /* magic高16位 */​
18    metaBuf[2] = (u16)(firmwareLen & 0xFFFF);            /* 长度低16位 */​
19    metaBuf[3] = (u16)((firmwareLen >> 16) & 0xFFFF);    /* 长度高16位 */​
20    metaBuf[4] = (u16)(crc32Value & 0xFFFF);             /* CRC低16位 */​
21    metaBuf[5] = (u16)((crc32Value >> 16) & 0xFFFF);     /* CRC高16位 */​
22    ​
23    /* 写入Flash元数据页（STMFLASH_Write内部处理擦除） */​
24    STMFLASH_Write(FLASH_META_ADDR, metaBuf, 6);​
25    ​
26    /* 回读验证写入是否成功 */​
27    STMFLASH_Read(FLASH_META_ADDR, verifyBuf, 6);​
28    for(i = 0; i < 6; i++)​
29
￼
    {​
30        if(metaBuf[i] != verifyBuf[i])​
31
￼
        {​
32            /* 写入失败，重试一次 */​
33            STMFLASH_Write(FLASH_META_ADDR, metaBuf, 6);​
34            break;​
35        }​
36#if DEBUG​
37        printf("IAP_WriteMeta verify OK\r\n");  /* 验证通过 */​
38#endif​
39    }​
40}
```

该函数就是将元数据写入到标志存储区的Flash里边，需要注意的就是CM3架构，它本身是小端存储模式(低字节存放低地址里边，metaBuf[0]就是低地址，metaBuf[1] = (u16)((FIRMWARE_MAGIC >> 16) & 0xFFFF)这行代码表示的意思是，将标志位的16位高字节先右移16位，再按位与0XFFFF得到其高16位)，这个不再过多叙述，之前的章节讲的有。所以此处组装元数据去写flash，本身就是为了契合CM3内核的存储方式。

#### 7.5.4 总结

￼
本次更新是为了解决固件升级后，再次复位重新进入boot区旧固件的问题。已更新为第一次固件升级成功后，无论之后上电复位多少次，会稳定执行APP区新固件，不会主动回退到boot区旧固件执行。如果上电复位后，主动回退到boot区旧固件执行，就是上一次固件升级失败了，比如说上一次固件升级时写APP固件到一半，板子突然掉电了。
如果需要强制系统回到boot旧固件运行，需要将宏ERASE打开，赋值为1。这里多说一点，防止有的粉丝朋友找不到，所有的宏开关都在sys.h头文件里边，具体如下代码所示：

```
/**************功能开关********************************/​
2/*APP区Flash擦除开关，0关闭，1打开*/​
3#define ERASE                   0​
4​
5/*调试宏开关,0关闭调试功能,1打开*/​
6#define DEBUG                   0​
7​
8/*固件升级功能开关,1打开,0关闭*/​
9#define ifopen                  0​
10​
11/*传感器数据调试宏*/​
12#define SENSOR_DEBUG            0​
13​
14#if ifopen​
15​
16    /*此处是因为笔者固件程序就这么大，请根据需要自行修改其大小，大小需为APP程序的code段加RO段加RW段加CRC字节长度--不甘心的咸鱼*/​
17    #define   buff_size         3692 ​
18    ​
19#endif​
20/**************功能开关********************************/
```

8. ### 项目高频面试点

  ￼

  #### 8.1 为什么在rtos的多任务间通信中，使用互斥量加全局变量的形式？

  ￼
  答：因为如果不使用互斥量的话，会导致结构体成员可能在写入一半时被其他任务抢占，导致读取到不一致的数据。因此，使用互斥量是为了保护共享数据，防止多个任务同时读写造成数据混乱。

  #### 8.2  那请你说一下为什么不采用队列的方式来进行任务间通信呢？

  ￼
  答：因为本项目中的数据模型需要共享最新状态，而不是历史数据；需要多读多写，关注的是最新值，用互斥量最为合适。且用互斥量加全局变量内存开销比较小，仅需一个结构体；而使用队列内存开销大，需要定义多个队列缓冲区。

  #### 8.3 说一下堆和栈的区别。

  ￼
  答：栈用于保存局部变量、函数调用和递归、函数的形参、通用寄存器等，空间由系统自动分配和回收，堆区用于保存动态分配的数据，如 malloc 函数申请 的内存数据就保存在堆区，其空间由程序员进行手动分配和释放。  其次，栈是一种线性结构，遵循后进先出原则；而堆是一种树状结构，没有 固定规则，允许随机插入和删除等操作。栈的大小是有限的，堆的大小可以根据 需求进行动态调整，但也受物理内存的限制。  最后，栈的分配和释放速度快，堆比较慢。

  #### 8.4 栈大小的单位是字还是字节，栈溢出会造成什么风险？

  ￼答：单位是字，因为栈大小定义的类型是32位的，32位是4个字节，1个字为4个字节；如果栈溢出会造成HardFault异常。

  #### 8.5 请你说一下FreeRTOS的启动流程？￼

  答：当上电复位后，内核从0x08000000处取出堆栈指针（根据启动方式不同，地址不同，一般在Flash启动），完成堆栈初始化；接下来在0X08000004 处取出复位中断的入口地址加载进PC寄存器，此时跳转到复位中断函数执行，在该函数里先进行系统时钟的初始化，然后调用__main函数将Flash里边存储的data数据拷贝到SRAM上，将BSS段变量进行清零。最后，调用main函数执行。在main函数中先完成必要的硬件初始化，然后创建开始任务完成RTOS的初始化，这包括：创建开始任务、TCB的初始化及任务堆栈初始化，将任务加入就绪表等操作。
  最后一步就是开启多任务调度，该函数会创建内核必需的空闲任务，若启用软件定时器则同时创建定时器服务任务。随后调度器被使能，并调用硬件移植层接口xPortStartScheduler，该接口配置PendSV和SysTick异常的优先级，启动系统滴答定时器，并通过触发SVC异常来激活首个任务。
  在SVC异常服务程序中，系统从第一个任务的任务控制块中恢复其上下文，包括堆栈指针和程序状态字。当异常返回时，CPU跳转到任务入口函数，至此系统正式进入多任务调度环境。后续所有任务由内核基于优先级和事件进行抢占式调度，而vTaskStartScheduler函数将永不返回。

  #### 8.6 请你说一下什么是Bootloader？STM32的启动上电流程是怎样的？

  ￼
  答：Bootloader叫做引导加载程序。在嵌入式系统上电复位后首先运行引导加载程序，它的功能主要是负责系统的上电自检、必要的硬件初始化、建立储存空间映射，并加载和启动操作系统。Bootloader一般储存在bootROM中，当前使用最多的类型是NOR flash rom，在大多数的嵌入式系统中，flash里边不仅储存了bootloader，还储存了用户程序代码。Bootloader有两种工作模式，一种是启动加载模式，另一种是下载模式；而本章节所提到的固件升级就属于bootloader的下载模式。
  STM32的上电启动流程：当STM32发生复位时，此时硬件会强制PC寄存器指向一个固定地址0X00000000（或者是由boot引脚映射的0x08000000），该地址存放的是主堆栈指针（MSP）的初始值，内核会读取该值并进行堆栈初始化，以创建好C环境以及安全调用中断服务函数。
  其次，PC寄存器会执行到0x00000004地址，取出该地址存放的复位中断处理函数，并跳转过去执行；在复位函数中，首先完成系统时钟的初始化，其次调用__main函数，完成数据的初始化，如将Flash中的data段数据拷贝到RAM中，然后将未初始化的全局变量（bss段）清零。最后，__main函数调用main函数，进入到main函数执行。

  #### 8.7 请你说一下移植rtos内核时需要对哪些文件进行修改？

  ￼
  答：移植RTOS时需要对以下文件进行修改：内核裁剪文件 FreeRTOSConfig.h（主要修改中断配置、主频配置以及内存分配等）、port.c文件（主要修改延时函数为RTOS的延时函数及 配置SysTick 定时器）、stm32f10x_it.c 文件（因为移植进来的RTOS文件已经实 现了PendSV异常函数、SVC异常函数及SysTick异常函数，因此需要将该文件 的这些函数注释掉，防止引起重复定义的报错）。

  #### 8.8 请你说一下直流有刷电机的驱动原理？

  ￼
  答：直流有刷电机的驱动硬件核心是H桥电路，而从嵌入式软件的角度来看，我们通过控制H桥上四个开关管的导通状态来实现方向与速度的精确管理。方向控制由软件直接操作两个GPIO完成：通过切换两组对角开关管的导通组合，改变流过电机的电流方向，从而控制电机正转或反转；同时，软件必须通过逻辑互锁或配置定时器的死区时间，确保同一桥臂的上下管不会同时导通，防止短路。
  ￼速度调节则采用单极性PWM调制。以正转为例，软件将一个开关管（如高端）设为常开，另一个对角管由定时器输出的PWM波驱动。通过动态更新定时器的比较寄存器来改变PWM占空比，即可调节电机两端的平均电压，实现线性调速。当PWM管关断时，电机电感产生的反电动势通过常开管和另一侧桥臂的续流二极管形成回路，确保电流连续。在FreeRTOS任务中，软件根据控制指令实时计算并更新PWM参数，并可能结合电流采样实现闭环稳速，从而完成精准、安全的电机驱动。

  #### 8.9 请你说一下编码器测速的原理？

  答：编码器测速的核心原理是将电机的旋转位移转换为脉冲信号，通过测量脉冲的频率或周期来计算出转速。主要利用MCU的定时器资源进行脉冲采集与解算。在实际应用中，最常见的做法是使用霍尔传感器，它输出两路相位相差90度的正交脉冲信号（通常称为A相和B相）。我们将这两路信号接入MCU的定时器输入引脚，并配置定时器为编码器接口模式。在此模式下，硬件会自动根据两路脉冲的相位关系判断旋转方向，并在每个边沿（上升沿和下降沿）进行计数，从而实现4倍频，提高测量精度。
  软件层面通过在固定时间窗口内捕获定时器的脉冲计数值来计算转速；并结合定时器的输入捕获和溢出中断来协同工作。在FreeRTOS任务中，我们定期读取定时器的计数值并进行清零或差分运算，经过滤波和单位换算后，即可得到实时的电机转速，用于后续的闭环PID控制。

  #### 8.10 请你说一下什么是PID算法，各个参数分别解决什么问题？

  ￼
  答：PID算法即比例-积分-微分控制，是一种基于反馈的闭环控制算法。它通过计算目标值（期望转速）与实际值（编码器测得的当前转速）之间的误差，并将误差的比例、积分和微分项进行线性组合，生成控制量（如PWM占空比）来驱动执行器，从而使系统快速、准确、稳定地达到目标值。
  比例（P）参数直接放大当前误差，其作用是迅速响应偏差，让系统向目标靠拢。P越大响应越快，但过大会导致超调和震荡，且单独使用无法消除静差。积分（I）参数对历史误差进行累积，主要解决稳态误差问题，确保系统最终能精确到达目标值；但积分过强会引起积分饱和和超调，需要配合限幅或抗积分饱和机制。微分（D）参数根据误差变化率提前施加修正，起到预测和阻尼作用，能有效抑制超调、提高系统稳定性。

  #### 8.11 请你说一下如何用C语言实现面向对象思想？

  ￼
  答：在C语言中实现面向对象编程，主要利用结构体和函数指针来模拟类的三大特性：封装、继承和多态。
  封装通过将数据成员和函数指针组合在一个结构体中实现。例如，定义一个“类”时，结构体包含状态变量，并包含指向相关操作的函数指针，使用者通过该结构体实例调用方法，隐藏内部实现细节。
  继承可以通过结构体嵌套来实现。将基类结构体作为子类结构体的第一个成员，这样在内存布局上，子类对象首地址与基类部分对齐，可以安全地将子类指针强制转换为基类指针，实现基类方法对派生部分的操作。
  多态则依赖于虚函数表（vtable）。在基类中定义一个包含函数指针的结构体作为虚表，基类实例中保存虚表指针。子类可以修改虚表中的函数指针指向自己的实现，从而实现运行时动态绑定，也就是多态。
  注：在本项目中，笔者使用的是封装思想。

  #### 8.12 请你说一下dht11单总线协议

  ￼
  答：DHT11单总线协议采用一根数据线与MCU连接，总线空闲时由上拉电阻保持高电平。通信由主机发起，整个过程需严格遵循时序：主机首先拉低总线至少18ms（通常18-30ms）以启动传输，然后释放总线并延时20-40us，随后检测从机响应。从机接收到起始信号后，拉低总线约80us作为应答，再拉高80us准备输出数据。
  数据传输时，每一位都以50us低电平开始，随后高电平的持续时间决定了该位的值：若高电平持续26-28us，表示“0”；若持续70us左右，则表示“1”。所有数据共40位，依次输出湿度整数部分（8位）、湿度小数部分（8位）、温度整数部分（8位）、温度小数部分（8位）和校验和（8位），校验和为前四个字节之和的低8位。接收完成后，主机需再次拉低总线并释放，以备下次通信。

  #### 8.13 请说一下SPI的通信原理以及工作模式

  ￼
  答：SPI是串行外围设备接口，是一种高速的同步、全双工通信 总线，仅支持一主多从；一般使用四根线进行通信：SCLK、MOSI、MISO及片 选线CS。  
  其通信原理是：主机和从机都有一个串行移位寄存器，主机通过向它的SPI 串行寄存器写入一个字节来发起一次传输。寄存器通过MOSI信号线将字节传送 给从机，从机也将自己的移位寄存器中的内容通过MISO信号线返回给主机。这 样，两个移位寄存器中的内容就被交换。  
  工作模式：其工作模式有四种，通过CPHA（时钟相位）、CPOL（时钟极性） 来决定。如果CPOL=0，串行同步时钟的空闲状态为低电平；如果CPOL=1，串 行同步时钟的空闲状态为高电平。时钟相位（CPHA）能够配置用于选择两种不 同的传输协议之一进行数据传输。如果CPHA=0，在串行同步时钟的第一个跳变 17  作者--不甘心的咸鱼（tb43915564）；另一个号：小红书—不搭（414192836）  沿（上升或下降）数据被采样；如果CPHA=1，在串行同步时钟的第二个跳变沿 （上升或下降）数据被采样。SPI主模块和与之通信的外设备时钟相位和极性应 该一致。

  #### 8.14 说一下编写PWM驱动的步骤

  ￼
  答：PWM是脉冲宽度调制，就是能控制脉冲的宽度。占空比是 高电平的持续时间占整个周期的比例。PWM的驱动代码如下：首先应该看手册 确定想用哪个定时器通道输出PWM波，因为定时器有四个通道可输出PWM波。 确定GPIO口以后，打开定时器和GPIO口的时钟，然后配置IO口的输出为推 挽输出，以及配置IO口速度，初始化IO口；接下来，配置定时器的重装值和预 分频值、计数模式、时钟分割及初始化定时器；接下来，开始设置PWM模式（有 边沿对齐和中心对齐两种模式），以及使能比较输出、设置输出极性来确定谁是 有效电平；接下来初始化定时器的PWM输出通道，如T3C3；最后，使能定时器。

  #### 8.15 说一下FreeRTOS的任务切换原理

  ￼
  答：RTOS任务切换实际上就是堆栈指针SP的切换。当需要发 生任务切换时，首先先保存当前任务的上下文，此时为入栈操作，将CPU的PC、 XPSR、LR 及通用寄存器保存在栈中，并且将CPU的堆栈指针保存在当前任务的TCB里边；接下来寻找最高优先级的任务，找到以后从最高优先级任务的TCB 里取出堆栈指针SP赋给CPU，由于此时SP指向内核，因此新任务的堆栈数据 就会恢复到CPU的各寄存器上，当CPU完成寄存器恢复并执行最后的返回指令 后，CPU就跳转到新任务上次被切换出去时指令的下一条地址继续执行，从而完 成了从一个任务到另一个任务的切换。 

  #### 8.16 什么是优先级反转？如何解决？

  ￼
  答：优先级反转发生在实时系统中，当一个低优先级任务持有了 某个共享资源（如互斥锁），而一个高优先级任务需要访问该资源时被迫等待。 此时，如果一个中优先级任务（其优先级高于持有资源的低优先级任务，但低于 等待资源的高优先级任务）开始运行（因为它不需要该资源且就绪），它将抢占持有资源的低优先级任务。这导致持有资源的低优先级任务无法执行，也就无法 及时释放资源，结果就是本该最高优先级运行的任务（等待资源者）被一个中优 先级任务间接地、长时间地阻塞，严重破坏了系统的实时性和优先级调度原则。  
  解决方法：采用优先级继承。当高优先级任务等着低优先级任务释放资源时， 此时将低优先级任务的优先级临时提到最高，当资源释放后，就能让高优先级任 务及时访问；此时低优先级任务的优先级恢复原样。

  #### ￼8.17 RTOS中的延时函数源码看过吗？如何实现调度的？

  ￼
  答：延时函数的底层原理是根据需要延时的时间，换算成时钟节 拍数；在延时的这段时钟节拍数里，将当前任务变成阻塞态，从而不参与任务调 度。然后去任务就绪表里寻找其他高优先级任务执行，当延时的时间到达以后， 再将该任务从阻塞态变成就绪态中，即加入任务就绪表中参与任务调度，当该任 务优先级最高时，延时时间到达后会立即切换回该任务执行。

  #### 8.18 RTOS中二值信号量与计数信号量的区别

  ￼
  答：FreeRTOS 中计数信号量与二值信号量的核心区别在于可用的信号数量上限及其适用场景：二值信号量本质上是一个开关，其计数值只能为 0 或1，主要用于表示单一资源（如一个设备）的可用性、或者作为一次性事件 发生的通知（如一个中断通知一个任务）；而计数信号量则拥有一个大于1的最 大计数值上限（创建时设定），其当前计数值可以表示多个相同资源的可用数量 （如缓冲池中有N个空闲缓冲区），允许多个任务在不冲突的情况下获取资源（计 数值减1），直到资源耗尽（计数值为0）才需等待。简单说，二值信号量是有/无状态，解决单一资源或事件同步；计数信号量是有多少个可用状态，管理多个同类资源实例。  
  举例说明：当发生一次中断事件时，此时可以用二值信号量存储这个事件， 但存储的该事件未被处理之前，又再次发生了中断事件，此时二值信号量就存不 住新的中断事件了，只能丢失掉；而计数信号量有大于1的存储空间，可以锁存 多个事件，避免了中断事件丢失问题。

  #### 8.19 请你说一下FreeRTOS中的动态内存管理方式

  答：frertos 中的动态内存，通过pvPortmalloc 和 VPortFree 接口提供五种分配方式: 
  ￼ 
  heap2:支持分配和释放，但只能分配释放固定大小的内存块，否则会引起内存碎片问题。   
  heap3:直接使用c语言标准库里边的malloc和free函数，但占用资源大。   
  heap4:freertos中默认的首选方案，能分配和释放，是 heap2 的升级版，可以合并空闲内存块减少内存碎片问题。但开销比heap1和heap2大， 分配速度较慢。    
  heap5:在 heap4 的基础上增加了对多内存区域的支持，可以让内存跨越多个 不连续的内存段。 可以将内部ram和外接ram合起来一起作为内存堆使用，但 其配置复杂且开销较大。
  ￼

  #### 8.20 请说一下什么是内存碎片

  ￼
  答：内存碎片指的是由于频繁分配和释放不同大小内存，导致内存不断分割，存在大量很小内存块，这种内存块由于太小没法再次分配，就成为了内存碎片。

  #### 8.21 说一下全局变量、静态全局变量、局部变量的存储区域及生命周期差异

  ￼
  答：全局变量和静态全局变量均存储在静态存储区（或者是全局区，一个意思）， 其中显式初始化的变量位于数据段（.data段），未初始化或初始化为零的变量位 于BSS 段（.bss），它们的生命周期贯穿整个程序运行期间，从程序加载开始到程序结束才释放。
  两者的主要区别在于链接作用域：全局变量默认具有外部链接性，可被其他源文件通过extern声明访问；而静态全局变量由于static修饰，具 有内部链接性，只能在其定义的源文件内部使用，有效避免了命名冲突。  局部变量则存储在栈区，其生命周期严格限定在所在函数或代码块的执行期 内，当函数调用结束或代码块退出时，栈被销毁，局部变量也随之自动失效，不再占用内存。这种动态分配的特性使得局部变量适合存储临时数据，但使用时必 须确保其生命周期不超出定义范围，否则会导致悬空引用。

  #### 8.22 说一下内存泄露的原因

  ￼
  答：内存泄漏的主要原因是对动态内存的不当管理，尤其是在资源受限的环境下。 常见情况包括：
  1、调用malloc、calloc 等函数分配内存后，未及时调用 free 释 放，导致已分配的内存块无法被再次使用；或者程序逻辑存在异常路径，使得释 放内存的代码未能执行（例如函数提前返回或发生错误时遗漏释放）。  
  2、如果释放内存后，仍有指针指向该区域（悬垂指针）并继续使用，虽然 不直接增加泄漏，但可能导致后续分配混乱；而反复分配内存却丢失了原始指针 （如指针被覆盖），则会使该内存块永远无法回收。 
  3、循环或递归中不断分配内存而未释放，以及第三方库或底层驱动未提供 对应的释放接口，最终造成可用内存逐渐耗尽，系统性能下降甚至崩溃。

  #### 8.23  GPIO 配置为输入模式时，上/下拉电阻的作用是什么？

  ￼
  答：在STM32 中，当GPIO配置为输入模式（上拉输入或下拉输入）时，上拉 和下拉电阻的核心作用是给引脚一个确定的电平状态，防止引脚悬空（即没有外 部信号驱动时）因外界电磁干扰导致电平不确定，从而引发逻辑误判。比如在使 用外部中断的时候，如果没有一个确定的初始电平状态，那中断自己由于外界的 电磁波干扰从而自行触发。此外，上/下拉电阻还用于匹配外部器件的电平需求， 如IIC总线的上拉或某些开漏输出器件的电平钳位，确保信号在空闲状态时处于 已知的逻辑电平。

  #### 8.24  裸机系统与RTOS系统的核心差异是什么？ 

  ￼
  答：裸机系统通常采用前后台架构，主程序在一个无限循环中顺序执行任务（后 台），而外部事件通过中断服务函数（前台）响应。这种模式下，所有任务共享 同一个CPU时间，任务的实时性依赖于主循环的周期和中断的优先级，难以处 理复杂的多任务并发，且随着功能增加，主循环的响应延迟可能变得不可控，代 码的模块化和维护性也较差。  RTOS 则引入了内核调度器，支持多任务并发执行，每个任务拥有独立的栈 空间和优先级，由内核根据调度算法（如抢占式或时间片轮转）动态分配CPU使 用权。这使得开发者可以将复杂系统拆分为多个独立任务，并通过信号量、消息 队列等机制实现任务间同步和通信。RTOS的核心优势在于处理多事件、多任务 的复杂嵌入式应用时，提升了系统的实时响应能力、资源利用率和代码可维护性。

  #### 8.25  RTOS 中消息队列的发送与接收过程，如何处理队列满/空的异常情况？

  ￼
  答：在RTOS中，消息队列通常作为环形缓冲区结合任务等待列表实现。发送消 息时，任务会调用队列发送API，若队列未满，系统将消息复制到队列中并检查 是否有任务在等待接收该队列的消息，若有则唤醒优先级最高的等待任务；若队 列已满，发送任务的行为取决于其指定的等待超时时间：如果超时时间不为 0， 任务将被挂起到该队列的等待发送列表中，让出CPU直到队列有空位或超时时 间到达，若超时时间到仍无空位则返回超时错误，若在此期间队列空出则被唤醒 并完成发送。接收消息的过程与之对称，当队列为空时，接收任务同样可以选择 阻塞等待或有数据到达时被唤醒，或者在超时后返回错误。这种机制使得任务间 能够高效同步，同时避免了轮询造成的CPU资源浪费。

  #### 8.26 中断响应的过程？

  ￼
  答：在STM32中，中断响应的过程始于外设或内核事件产生的中断请求信号， 该信号被发送至嵌套向量中断控制器NVIC。NVIC 首先检查该中断的使能状态 以及当前优先级是否高于正在执行的中断或主线程的优先级，如果条件满足，则 向处理器内核发出中断请求。处理器在完成当前指令后响应中断，硬件自动将关 键寄存器（包括程序计数器PC、状态寄存器xPSR、通用寄存器R0-R3、R12等） 的值压入当前栈（主栈或进程栈），然后从中断向量表中取出对应中断服务函数 ISR 的入口地址，并跳转执行。  
  进入ISR后，用户编写的处理代码开始执行，进行必要的硬件状态清除或数 据处理。ISR执行完毕后，通过专用的中断返回指令触发硬件出栈操作，恢复之 前压入栈中的寄存器值，使程序返回到被中断打断的位置继续执行。整个过程由 硬件自动完成上下文保存和恢复，确保了中断响应的实时性和可靠性。 

  #### 8.27 RTOS 的内存池机制如何优化内存分配？ 

  ￼
  答：RTOS中的内存池机制通过预先将一大块连续内存划分为多个固定大小的内 存块进行管理，从而有效优化了内存分配的性能和可靠性。系统启动时，内存池被初始化成一个由链表连接的块集合，每个块大小一致。当任务请求内存时，分 配算法只需从空闲链表中取出一个块，时间复杂度极低且具有确定性；释放时只 需将块重新挂回链表。这种设计从根本上消除了传统动态内存分配（如malloc） 产生的外部碎片问题，因为所有块大小固定，任何分配和释放都不会导致内存空 间被分割成不连续的小区域。  内存池的缺点是可能存在内部碎片，即如果请求的内存小于块大小，剩余的 字节就被浪费了，因此需要根据实际应用的数据结构大小来合理配置内存池的块尺寸。

  #### 8.28 FreeRTOS 中的中断服务函数与任务间通信的安全方式，为何不建议在ISR中 直接调用复杂函数？

  ￼
  答：在FreeRTOS 中，确保中断服务函数与任务间通信安全的核心是使用专为中 断设计的“FromISR” 结尾的 API 函数，如 xQueueSendFromISR 或 xSemaphoreGiveFromISR。这些函数在实现上避免了可能引起阻塞的操作，不会 试图获取内核锁或等待资源，同时它们会通过一个pxHigherPriorityTaskWoken参 数来指示是否有更高优先级的任务因本次通信而就绪，以便在中断退出时由调度 器决定是否触发任务切换，从而保证数据传递的原子性和实时性。  
  ￼不建议在 ISR 中直接调用复杂函数的主要原因在于中断上下文的高度受限 性。ISR 需要快速执行以释放 CPU 响应其他中断，若调用复杂或可能阻塞的函 数，将显著增加中断延迟，影响系统实时性。此外，许多复杂函数并非可重入， 且可能依赖临界区或调度器锁，在中断中调用容易破坏内核数据结构或导致死锁。 因此，最佳实践是让ISR仅完成最必要的快速操作（如清除中断标志），然后通 过安全机制将事件通知给任务，将耗时处理转移至任务上下文中。 

  #### 8.29  RTOS中任务优先级的分配原则，高优先级任务长期占用CPU会导致什么问题？

  答：任务优先级的分配通常遵循紧急事件快速响应与资源合理共享的原则。一般而言，对实时性要求高、执行时间短且需要快速响应的任务（如中断处理后的数 据接收、按键扫描等）应赋予较高优先级；而计算量大、耗时较长或对实时性要 求不高的任务（如数据显示、串口打印等）应赋予较低优先级。同时，需注意避 免多个高优先级任务频繁抢占导致低优先级任务“饿死”，并合理使用互斥量、任 务通知等机制来防止优先级反转问题。 
  如果一个高优先级任务长期占用CPU而不进入阻塞态（例如在任务循环中 未调用延时或等待事件），则会导致所有比它优先级低的任务永远无法获得CPU 使用权，这种现象称为“任务饥饿”。低优先级任务即使已准备就绪，也会因无法 被调度而永远无法执行，导致系统功能失效。RTOS的心跳节拍（如滴答定时器 处理）也可能被阻塞，最终引发看门狗超时复位或系统完全失去响应。 

  #### 8.30 固件升级过程中突然断电，再次上电如何保证系统能恢复并重新启动升级流程？

  答：在固件升级过程中突然断电后，系统能够恢复并重新启动升级流程的核心依 赖于双区备份机制和启动加载程序的安全设计。常见的实现方式是采用 A/B 分 区策略，即芯片内部至少包含两个独立的固件存储区（分区A和分区B）以及一 个启动引导区。升级过程中，新的固件被写入当前未使用的备份分区（例如分区 B），而原有的固件仍在分区A正常运行。如果断电发生在分区B写入期间，原有分区 A 的固件完好无损，上电后引导程序通过校验标记或状态标志发现升级 未完成，会自动回滚至分区A启动，并重新触发升级流程。  
  此外，还可以结合非易失性存储器中的状态标记来实现断点续传。每次升级 开始前，系统会在Flash中写入特定的升级标志和进度信息。若写入新固件过程 中断电，上电后启动加载程序首先检查这些标志，发现上次升级未完成且当前活 动分区不可用或校验失败，则会重新尝试从备份区启动或从通信接口（如云端、 U盘）重新获取固件包。部分高级设计还支持从异常中恢复，通过冗余的启动代 码强制进入固件更新模式，确保设备永不“变砖”，直至升级成功。
  PS：本次高频面试点仅针对本项目而言，如果需要更加全面的，请拍笔者的嵌入式八股文合集。
  ￼

9. #### 调试记录

  ￼
  1、关于编码器测速时，lcd屏幕上显示转速为1176 PRM,但实际转速达不到这个值，经检查原来
  是因为编码器测速时，其采样精度是50ms一次，但由于中断是1ms触发一次，每次中断会计数，直到计数50开始计算速度，但是此处我的设计思想是触发1ms中断时，设置标志位，然后再主函数里判断标志位生效时再去计算速度，最后加上串口函数及lcd显示函数显示速度值。这样的逻辑会使得第一次中断计数结束后到下一次中断计数前会有时间损耗，该时间损耗为串口函数和lcd显示函数所占时间，大约为5ms。使得5*50ms时才能计算一次速度，而理论的采样精度为50ms，则计算出来的速度为理论值的5倍。

2、关于电机的转向控制，问题为无论如何设置，电机仅能正转，不能反转；经检查发现是因为方向改变驱动函数的标准库驱动函数写错了，里边仅有C1通道的enable/disable，而没有CN1通道的enable/disable。导致无论如何设置，电机仅能正转，不能反转。

3、PID调参：再调节比例参数时，就要尽量让实际值达到目标值，再去调积分，不能仅关注曲线的完美度，从而让比例系数过低，此时会造成当目标值调高以后，实际值依然维持到原有目标值，不会达到新的目标值。

4、风速算法：之前选取的温湿度、气体浓度基准值过高，使得输出的PWM占空比过小，不足以驱动电机；因此将其基准值调整以输出合适占空比。

5、硬件初始化函数顺序搞错，导致电机无法转动；需要将串口初始化函数放在电机初始化函数之后。
￼
     
6、动态阈值算法错误，现象为当气体浓度依然在大于正常阈值时，电机却已关闭，原因是切换成高阈值后，此时检测标志为0，因此关闭电机；但浓度依然大于正常阈值，因此修改实现逻辑为必须气体浓度小于正常阈值时，电机才能关闭。     
7、调试CRC32校验时，遇到校验失败的问题。然后通过串口打印，发现：新固件的附加CRC码与接收端CRC32算法计算出来的CRC码、以及接收端分离出的CRC码，这三者值都对不上。分析如下：新固件的附加CRC码与CRC32算法计算出的新CRC码可能是算法问题，但新固件的附加CRC码与接收端分离出的CRC码理论应该可以对应，如果对应不上就说明有数据遗漏；
因此，查看了串口加DMA接收端源码，发现RAM内存缓冲区数组定义过小（3688），但由于这是新固件，附加了4字节的CRC码，因此内存缓冲区大小应该更改为3692才对。更改后可以对应，但与CRC32算法计算出的新CRC码依然无法对应，检查了算法实现没问题之后，剩下只能检查CRC的多项式生成表，发现有部分错误，随即修复，最终校验成功。

